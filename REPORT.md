# REPORT

실험 결과 기술 보고서. 설계 배경·수식 유도는 [IDEA.md](IDEA.md), 차별점 요약은 [NOVELTY.md](NOVELTY.md),
현재 작업 상태는 [PROGRESS.md](PROGRESS.md) 참조. 원본 로그: `work_dirs/*/eval.txt`, `work_dirs/*/train.log`.

## 1. 방법 요약

프레임을 16×16 패치로 분할 → 변화 감지(MSE, hysteresis+dilation+keyframe) → 마스크 $M_t$를 Mamba
이산화 파라미터에 곱함($\tilde\Delta = M\cdot\Delta$) → $M=0$인 패치는 hidden state가 정확히
이전 값으로 복사됨(연산 스킵). T-Mamba(시간축)/S-Mamba(공간축) 교차 적층 백본 + 경량 conv 디코더.
전체 유도는 IDEA.md §3.

## 2. PoC 실험 설정

- 모델: `SOKKANAEM` 기본 dim, size 128, clip_len 4
- 데이터: Virtual KITTI 2 전체 (100 시퀀스, 42,520 프레임, 21,120 클립), holdout=Scene06
- 학습: `configs/vkitti2.toml`, 30k step, RTX 4090 ~111분, ckpt 11MB
- 평가: 5,260 val 클립 중 100 샘플 (§4.5 표), 별도 ablation은 32프레임 클립·660개 프로토콜(`work_dirs/vkitti2/eval.txt`) — **절대 수치가 다른 표와 다름**, median scaling 방식 차이 때문 (클립당 1회 스케일링, 프레임 수에 따라 절대값 이동). 항목 내 상대 비교만 유효.

## 3. 결과

### 3.1 Go/No-Go — pixel gating (기준 판정)

| $\tau_{on}$ | active% | AbsRel | RMSE | $\delta_1$ | t-delta |
|---|---|---|---|---|---|
| 0 (풀 연산) | 100.0 | 0.2054 | 12.93 | 0.684 | 0.1033 |
| 0.05 | 68.1 | 0.2054 | 12.93 | 0.684 | 0.1002 |
| 0.1 | 44.6 | 0.2057 | 12.93 | 0.684 | 0.0962 |

**판정: Go.** 로드맵 기준(스킵 50%에서 AbsRel 열화 5% 이내)을 크게 상회 — 스킵 55%(active 44.6%)에서
AbsRel 열화 +0.15%(상대). 주행(전역 모션) 데이터에서조차 스킵 비용이 사실상 0.
스킵률↑에 따라 t-delta(시간 불안정성)도 단조 개선(0.1033→0.0962) — Δ-gating의 정확한 상태 복사가
후처리 없는 시간 일관성을 준다는 근거.

### 3.2 GMC + Feature-level Gating (ego-motion 확장, §3.5)

| $\tau_{on}$ | active% | AbsRel | RMSE | $\delta_1$ | t-delta |
|---|---|---|---|---|---|
| 0.2 | 54.1 | 0.2060 | 12.94 | 0.684 | 0.0985 |
| 0.4 | 23.7 | 0.2068 | 12.94 | 0.682 | 0.0918 |
| 0.8 | 2.3 | 0.2098 | 12.93 | 0.677 | 0.0762 |

Pixel gating의 스킵 바닥(~active 45%)을 GMC+feature gating이 돌파 — active 23.7%에서 AbsRel +0.7%,
극단(active 2.3%)에서도 $\delta_1$ 손실 0.007. 호모그래피 정렬이 ego-motion 유발 변화를 흡수해
잔차 변화만 감지됨을 확인. GMC 자체 오버헤드는 +0.8ms/frame (§3.3 wall-clock 참조).

### 3.3 Wall-clock 성능 (RTX 4090)

**128px, batch 1, 스트리밍 300프레임:**

| 모드 | active% | ms/frame | FPS |
|---|---|---|---|
| pixel gating, $\tau$=0 (풀 연산) | 100.0 | 2.69 | 372 |
| pixel gating, $\tau$=0.1 | 56.2 | 2.68 | 373 |
| GMC, $\tau$=0.4 | 33.4 | 3.50 | 286 |
| GMC, $\tau$=0.8 | 12.1 | 3.47 | 288 |

- 최초 프로파일: spatial 블록 Python 순차 스캔이 지연의 95–99%. **청크 segment-sum 스캔**(Mamba-2
  스타일, 수식 동일)으로 교체 → 104 → 372 FPS (**3.6×**, 128px), 512px에서 137→19ms/frame (**7×**).
- **CUDA graph 캡처**(`enable_cuda_graphs()`, full-compute 경로만): 128px 370→**1075 FPS**(2.9×),
  256px 154→**366 FPS**(2.4×). 512px는 compute-bound라 무이득. eval 지표 소수 4자리까지 동일 확인(수치 안정성 검증됨).
- **Static-patch 출력 캐싱**(`--spatial-cache`, phase 3, opt-in, **근사** — temporal Δ-gating과 달리
  static 패치의 문맥 기여를 생략):

  | 지점 (GMC, vkitti2) | active% | FPS 128px | FPS 512px | AbsRel | t-delta |
  |---|---|---|---|---|---|
  | 캐시 없음, $\tau$=0.8 | ~12–33 | 292 | 40.2 | 0.2098 | 0.076 |
  | 캐시 + $\tau$=0.8 | ~12–33 | **516** | **70.7** | 0.2185 | **0.068** |
  | 캐시 + $\tau$=0.2–0.4 (중간 active) | 54–72 | 316–374 | 34–41 | 0.235–0.245 | 0.54–0.79 |

  저-active(≤15%) 구간에서만 유효(1.8× 속도, AbsRel +4%, t-delta 오히려 개선). 중간 active%(40–80%)는
  fresh/stale 토큰 경계 문맥 불일치로 t-delta 6× 폭증 — **U자형 악화**, 이 구간은 켜지 않음. 주 타깃인
  고정 CCTV(active 5–10% 상시)가 정확히 캐시 최적 구간. 학습은 항상 풀 spatial(inference-only 최적화).

### 3.4 경량 모델 대비 (256px급, fp16+compile, batch 1, RTX 4090)

| 모델 | 파라미터 | FPS |
|---|---|---|
| DA V2 Small | 24.8M | 380 |
| DPT-SwinV2-Tiny | 40.9M | 371 |
| DA V1 Small | 24.8M | 280 |
| **본 모델 (풀연산+그래프)** | **2.8M** | **366** |
| 본 모델 (정적 스트림+캐시) | 2.8M | 460 |
| 본 모델 (128px 그래프) | 2.8M | 1075 |

~~단일 GPU FPS는 동급~~ — **이 표는 v3 시절 수치이고 양쪽 다 낡았다. §4.27b를 인용할 것**:
융합 커널 이후 본 모델은 fp16 compiled에서 2646 FPS, DA v2 Small은 1226 FPS로 2.2배 앞선다.

### 3.5 Ablation

**Keyframe 주기 $K$ / 변화 감지기 (eval-only, 32프레임 클립, 60클립, `work_dirs/vkitti2/eval.txt`):**

| 항목 | active% | AbsRel | $\delta_1$ | t-delta |
|---|---|---|---|---|
| K=5 | 73.7 | 0.2888 | 0.5338 | 0.1185 |
| K=10 (config 기본) | 69.2 | 0.2890 | 0.5332 | 0.1178 |
| K=30 | 66.5 | 0.2891 | 0.5332 | 0.1173 |
| K=1000 | 65.1 | 0.2892 | 0.5331 | 0.1166 |
| cosine $\tau$=0.3 (iso-active, MSE 0.05 대응) | 59.8 | 0.2897 | 0.5313 | 0.1180 |

K=5→1000 구간 AbsRel 변화 <0.001 — 32프레임 범위 드리프트 실질 0. MSE vs cosine도 동급(감지기 선택 둔감,
싼 MSE 유지 결정). K=10은 보수적, K=30이면 active 2.7%p 추가 절감 공짜.

**장기 스트림 드리프트** (270프레임×4 시퀀스, Scene06 스트리밍): keyframe 완전히 꺼도($K=\infty$)
late-frame AbsRel이 early 대비 **−3.8%(개선)** — 저속 드리프트 리스크(IDEA.md §5) vkitti2 스케일에서 미발현.
스트리밍 실측 active 20–28%($\tau$=0.05) vs 클립 평가 68% — **클립 평가가 스킵률을 심하게 과소평가**함(클립마다
keyframe 리셋). 실배포 이득은 클립 지표보다 큼.

**마스크 분포 3-arm** (각 30k step, Scene06 홀드아웃, $\tau$=0.05, `work_dirs/abl-*/eval.txt` 실측):

| 학습 마스크 | AbsRel | $\delta_1$ | t-delta |
|---|---|---|---|
| **iid random, max_skip 0.5 (채택)** | **0.3272** | **0.4271** | 0.0932 |
| 없음 (max_skip 0) | 0.3326 | 0.4290 | **0.9312** |
| detector-driven 10k fine-tune | 0.3614 | 0.4173 | 0.2732 |
| iid random, max_skip 0.8 | 0.3952 | 0.3613 | 0.0783 |

결론: (1) 마스크 학습의 진짜 기여는 시간 안정성(없으면 t-delta 10×, 정확도는 Δ-gating 수식이 그냥 보장).
(2) 과도한 스킵 학습(0.8)은 gradient 부족으로 정확도 붕괴. (3) "학습-배포 분포 일치" 가설 **기각**
— detector 마스크(공간 상관 blob)가 iid 무작위성보다 못함, iid 자체가 regularizer 역할.
**본 학습 레시피로 iid random @ max_skip 0.5 확정.**

## 4. 본 학습 (main.toml) 현황

- 데이터: vkitti2 + tartanair2 + pointodyssey 혼합, 256px, 100k step 목표

### 4.6 v1 붕괴 사건 (2026-07-24)

v1은 100k step 완주했으나 **holdout eval에서 모든 픽셀·모든 프레임이 상수(4.85)** 임을 발견 —
AbsRel 0.80, δ1 0.115, t-delta 정확히 0.0000. 원인: Kendall auto-loss-weight가
`temporal_loss`(연속 프레임 depth 동일성) 가중치를 clamp 상한(exp(8)≈2981×)까지 밀어붙임 —
이 loss는 **상수 출력이 정확한 전역 최솟값**이라, 가중치가 그 정도로 쏠리면 실제 추정을
포기하고 상수로 붕괴하는 게 수학적 최적. 과거 모든 ablation(§3.5 표 포함, 고정가중치
1/0.5/0.1)은 이 문제 없었음 — auto_loss_weight 하나가 원인. v1 전량 폐기
(`work_dirs/main-COLLAPSED-autolossweight-20260724`).

부수적으로 holdout eval 준비 중 tartanair2 10개 env 중 6개가 압축 이중 중첩으로 어댑터에
전혀 안 잡히던 버그도 발견·수정 (44→74 시퀀스, 총 clip 160,973→195,475). v1 학습 자체는
이 데이터 없이 진행됐던 것이라 상수-붕괴와는 별개 결함. 상세: [PROGRESS.md](PROGRESS.md).

### 4.7 v3 결과 (2026-07-25, 100k step 완주)

auto_loss_weight 제거 + 고정가중치(si_log + 0.5·grad + 0.1·temporal + 0.05·normal) +
붕괴 자동감지(`sokkanaem/collapse.py`) + 손상 프레임 방어 로더(`sokkanaem/data.py`,
재시도 후 랜덤 폴백)로 재학습. 195,475 clips, 256px, depth_std 학습 내내 건강(대략
2~34 범위에서 변동, 붕괴 없음). 학습 중 `tartanair_v2/Hospital` 일부 프레임에서 간헐적
읽기 실패(`OSError: image file is truncated`) 발생 — 별도 재검사에서 해당 파일들은
전부 정상 로드돼 **영구 손상이 아니라 `num_workers=4`의 `/archive` 동시 접근으로 인한
일시적 오류**로 추정, 방어 로더가 매번 정상 폴백 처리함(크래시 없음).

**Holdout eval** (vkitti2 Scene06 + tartanair OldTownFall + pointodyssey val/test,
8,929 clips, 256px, clip_len 8, 100 샘플, `work_dirs/main/eval.txt`):

| $\tau_{on}$ | active% | AbsRel | RMSE | $\delta_1$ | t-delta |
|---|---|---|---|---|---|
| 0 (풀 연산) | 100.0 | 0.3822 | 31.61 | 0.394 | 0.2143 |
| 0.005 | 29.0 | 0.3811 | 31.46 | 0.397 | 0.1666 |
| 0.01 | 26.6 | 0.3813 | 31.46 | 0.397 | 0.1657 |
| 0.02 | 23.0 | 0.3815 | 31.47 | 0.397 | 0.1638 |
| 0.05 (config 기본) | 16.6 | 0.3819 | 31.51 | 0.397 | 0.1586 |
| 0.1 | 11.2 | 0.3820 | 31.51 | 0.396 | 0.1535 |

핵심 패턴 재확인: active% 100→11.2로 떨어져도 AbsRel/δ1 사실상 그대로(0.382/0.394 근방
유지), t-delta는 오히려 개선(0.214→0.154) — PoC(§3.1 표)와 같은 방향, 이번엔 3개
합성 데이터셋 혼합·홀드아웃 환경(unseen scene/env)에서도 재확인됨. 절대 정확도
($\delta_1$ 0.40, AbsRel 0.38)는 PoC의 vkitti2 단독 128px 결과($\delta_1$ 0.68)보다 낮음 —
홀드아웃이 훨씬 다양하고 어려운 멀티도메인 unseen 평가라 직접 비교 불가, 절대 성능은
추가 학습(더 많은 step, 데이터 정제)으로 개선 여지 있음.

**EMA vs raw 가중치**: 사실상 동일 (AbsRel 0.38201 vs 0.38191, $\delta_1$ 0.39677 vs
0.39671). LR이 cosine으로 학습 후반 거의 0까지 떨어져 막판엔 가중치가 거의 안 움직였고,
그래서 EMA가 raw를 그냥 따라잡은 상태 — 이 런에서는 EMA로 인한 손해도 이득도 측정 안 됨.

### 4.8 외부 SOTA 모델과의 비교 (2026-07-25)

IDEA.md §4.3 베이스라인 후보 중 **같은 holdout split(8,929 clips)·같은 protocol**로
직접 돌려본 결과 3개. NVDS는 정식 배포처를 못 찾아 스킵. 나머지 셋은 전부 transformers
호환이 안 돼(DA v2만 예외) 원본 코드가 필요 — 환경 오염 방지 위해 별도 conda env
2개(`baselines`: DA v2·DA3용 torch 2.8.0, `vda`: Video Depth Anything 전용 torch
2.1.1 하드 pin이라 별도 분리)에서 실행, `sokkanaem` 학습 env는 안 건드림
(`scripts/eval_baseline_da2.py`, `eval_baseline_da3.py`, `eval_baseline_vda.py`).

세 모델 모두 relative/metric 여부가 달라 정합 방식을 각각 실측으로 확인 후 적용:
- **DA v2**: relative disparity (1/GT depth와 상관 0.988, GT 자체와는 -0.64) →
  클립 단위(8프레임 전체 valid 픽셀) scale+shift 최소자승 정합 후 depth로 역변환.
- **DA3**: depth-like (GT와 상관 0.886) → 역변환 없이 클립 단위 scale+shift 직접 정합.
  8프레임을 한번에 `model.inference()`에 넣는 any-view 모델이라(순차적 video 모델 아님)
  구조적으로 어느 정도 프레임 간 일관성을 공짜로 가져감.
- **Video Depth Anything (metric, Small)**: 명목상 metric이나 median 비율이 0.74라
  약간의 스케일 보정 필요 → SOKKANAEM과 동일하게 median-scale만(더 엄격한 정합) 적용.
  **셋 중 유일하게 명시적 시간축 메커니즘(`motion_module`)을 가진 진짜 video 모델.**

| 모델 | 파라미터 | 시간 메커니즘 | AbsRel | RMSE | $\delta_1$ | t-delta |
|---|---|---|---|---|---|---|
| SOKKANAEM v3 ($\tau$=0.05) | 2.8M | Δ-gating (exact) | 0.3819 | 31.51 | 0.397 | **0.1586** |
| Depth Anything V2 Small | 24.8M | 없음(프레임독립) | **0.1223** | 56.79 | 0.898 | 7.9063 |
| Depth Anything 3 Base | 0.12B | any-view(비순차) | 0.2735 | **16.79** | 0.562 | 2.0443 |
| Video Depth Anything Small (metric) | 28.4M | motion module | 0.1615 | 30.93 | **0.8705** | 2.7184 |

**해석**:
1. **단일 프레임 정확도는 파라미터 규모 순서 그대로** — DA v2(24.8M) > VDA(28.4M) ≈
   DA3(120M, 단 아키텍처가 depth 전용이 아니라 pose까지 포괄해 직접 비교는 조심스러움) >
   SOKKANAEM(2.8M). 예상대로, 놀랍지 않음.
2. **시간 안정성은 전부 SOKKANAEM이 압도** — 심지어 명시적 시간축 모듈을 가진 **VDA조차
   t-delta 2.72로 SOKKANAEM(0.159)의 약 17배**. DA3(any-view, 8프레임 동시 처리)는
   2.04로 DA v2(7.91)보단 낫지만 역시 SOKKANAEM엔 한참 못 미침. **"명시적 시간 모듈이
   있어도 소프트 스무딩일 뿐, Δ-gating의 '정확한 상태 복사'와는 질적으로 다르다"**는
   가설을 세 모델 전부에 대해 실증 — NOVELTY.md 기여 2의 가장 강력한 근거.
3. RMSE는 DA3가 제일 낮음(16.8) — scale+shift 정합이 median-scale보다 극단값에
   민감하지 않아서로 보이며, AbsRel/δ1이 더 신뢰할 만한 정확도 지표.

**한계**: 파라미터 수·학습 데이터 규모가 자릿수로 다름 — "정확도 경쟁"이 아니라
"같은 프로토콜에서 정확도 대 시간일관성이라는 축 자체가 다르다"는 걸 보여주는 비교.
DA3는 pose estimation까지 포함한 any-view 모델이라 순수 monocular-video 모델과 1:1
비교는 아님. 세 baseline 모두 이번이 처음 이 holdout으로 평가된 것이라 재현성 확인은
1회만 실시.

### 4.9 정확도 개선 시도 (2026-07-25~26)

§4.8에서 확인된 정확도 격차(δ1 0.40 vs 외부 0.56~0.90)를 메꾸려 두 가지 시도.

**v4 — DINOv2 feature distillation (`distill_weight=0.1`, `sokkanaem/distill.py`)**:
frozen DINOv2-small(22M)의 patch feature를 최종 백본 토큰이 cosine loss로 따라가게
학습(추론 비용 0). 결과 — **효과 없음**:

| | AbsRel | RMSE | $\delta_1$ | t-delta |
|---|---|---|---|---|
| v3 (distillation 없음) | 0.3819 | 31.51 | **0.397** | 0.1586 |
| v4 (distillation) | 0.3873 | 30.81 | 0.384 | 0.1299 |

δ1 오히려 소폭 하락, AbsRel 거의 그대로. RMSE/t-delta만 소폭 개선. 원인 추정:
(1) distill 대상이 T/S-Mamba 블록을 전부 거친 **최종** 토큰이라 이미 depth 특화된
표현을 범용 DINOv2 semantic feature로 되돌리려는 힘이 경합만 유발했을 가능성,
(2) DINOv2 feature 자체가 depth 특화 표현이 아니라 "표현력 부족"이라는 가설 자체가
틀렸을 가능성. **v5부터는 `distill_weight=0`(off)으로 되돌림** — 재시도하려면 초기
임베딩 단에서 distill하거나 가중치를 다르게 스윕해야 함, 지금은 보류.

**v5 — 모델 용량 증가 (`dim` 192→384, 진행 중)**: 속도 스윕 결과 depth·d_state가
dim보다 unit당 훨씬 비쌈(dim256/depth6/d_state32=6.95M이 dim384/depth4/d_state16=9.92M
보다 2배 이상 느림) — dim만 올림. 9.92M, 외부 baseline 중 가장 작은 DA v2(24.8M)보다도
작음. 256px 구간 ~2.4배 느려짐(693ms vs 287ms/step), 128px 커리큘럼 구간은 오히려
더 빠름(154ms) — 추정 총 소요 ~16h(vs v3/v4의 ~8h). 결과는 완주 후 갱신.

### 4.10 지표 프로토콜 정정 및 시간 지표 보강 (2026-07-26)

논문화 준비 중 §4.8 헤드라인 비교의 프로토콜 결함 2건을 찾아 고쳤다.

1. **t-delta 스케일 불일치**: `scripts/eval.py`는 t-delta를 **raw 출력**에서, 세 baseline
   스크립트는 **median-scaled 출력**에서 계산하고 있었다. v3의 median scale은 0.70~1.11
   (평균 0.77)로 1이 아니므로 두 수치는 같은 양이 아니었다. 실측 영향은 0.95배(17× 주장은
   유지)지만 프로토콜은 동일해야 하므로 scaled 기준으로 통일.
2. **t-delta는 상수 출력이 전역 최적**이라는 구조적 약점(§4.6 붕괴가 정확히 0.0000을 기록).
   flow warping만으로도 안 풀린다 — 공간적으로 상수인 필드는 어떤 warp에도 불변이라 OPW도
   0이 된다. 그래서 `sokkanaem/metrics.py`에 **두 지표**를 추가:
   - **OPW**: RAFT(`raft_small`) 광학흐름으로 $D_t$를 $t+1$로 워핑한 뒤의 자기 일관성 오차.
     선행연구(NVDS/VDA)와 이름이 통하는 표준 지표. 여전히 상수에 취약하므로 정확도와 함께만 읽는다.
   - **TCE**: 같은 워프 잔차를 **GT 자신의 워프 잔차 기준으로** 측정. 상수 예측은 GT의 실제
     프레임 간 기하 변화를 전부 벌점으로 받고, 완벽한 예측은 0 — 퇴화하지 않는 시간 지표.
     동일한 워프·마스크를 모든 모델에 그대로 적용하므로 비교는 공정.
   - **const 제어행**(`--control`): 클립별 최적 상수 예측을 한 행으로 병기해, t-delta/OPW가
     게임 가능함을 표 안에서 실증한다.

   TAE(pose 워프)는 미구현 — 세 어댑터가 extrinsic/intrinsic을 안 싣고 있어 데이터셋별 배관이
   필요하다. 검증: `tests/test_metrics.py`(상수 예측이 t-delta·OPW는 통과하고 TCE에서만 걸림).

### 4.11 FLOPs 회계 — 기여 3 재평가 (2026-07-26)

§4.2가 요구한 "변화율 대비 FLOPs 곡선"을 처음 계산(`scripts/flops.py`, config에서 유도되는
해석적 MAC 카운트). **결과는 기여 3("연산량 ∝ 변화율")을 지지하지 않는다.**

256px, dim 192(v3) 기준 프레임당 2.337 GMAC 구성: **디코더 67.8%**, spatial 21.8%,
temporal 8.7%, embed 1.6%.

| active% | GMAC (Δ-gating) | vs 풀연산 | +spatial cache | 이상적(비례) |
|---|---|---|---|---|
| 100 | 2.337 | 100% | 100% | 100% |
| 16.6 | 2.266 | **97.0%** | 78.8% | 16.6% |
| 0 | 2.252 | **96.4%** | 74.6% | 0% |

원인 두 가지:

1. **디코더가 IDEA §3.3의 자체 예산을 22배 위반**. "디코더 예산은 백본의 10% 이하"라고
   써놓고 실제로는 백본의 2.2배 — upsample을 먼저 하고 64→32, 32→1 conv를 **풀 해상도**에서
   돌리는 구조 때문(256×256×64×32×9 = 1.21 GMAC 한 층). 이만한 dense 블록이 있으면 패치를
   아무리 스킵해도 총 FLOPs가 안 움직인다.
2. **Δ-gating이 제거하는 건 static 토큰의 state 경로뿐**. $\tilde\Delta=0$은 `dt_proj`·B
   절반·순환식을 없애지만, 블록은 여전히 **보존된 state를 현재 프레임의 C로 읽어야** 하므로
   `in_proj`/`out_proj`/C가 남는다 — static 토큰이 active 토큰의 **58.5%** 비용을 계속 낸다.
   정확성(exactness)은 state의 성질이고, readout을 생략할 면허가 아니다. S-Mamba는 애초에
   temporal state가 없어 Δ-gating 대상 밖이며, 유일한 절감 수단이 근사인 static 출력 캐시다.

이것이 §3.3 wall-clock 표의 미해명 수치를 정확히 설명한다: pixel gating $\tau$=0.1에서
active 56%인데 372→373 FPS(이득 0). 그 표의 실제 이득은 전부 청크 스캔·CUDA graph·캐시에서
나온 것이고 **게이팅 자체의 속도 기여는 0**이었다.

추가 실측(4090, 256px, eager, 학습과 GPU 공유): step 13.9ms 중 디코더 0.42ms(**3%**)로
벽시계에서는 디코더가 병목이 아니다 — 즉 이 모델은 FLOP-bound가 아니라 launch-bound이며,
그래서 FLOPs 절감도 스킵 절감도 4090 FPS에 그대로 나타나지 않는다. (로드맵 3단계의 Triton
블록 희소 커널이 벽시계 쪽 정답.)

**대응(v6, `configs/main_v6.toml`)**: (a) `ShuffleDecoder` — 채널 연산을 전부 패치 해상도에서
하고 pixel-shuffle로 업샘플, 풀 해상도에는 1→16→1 경계 보정만 남김(디코더 비중 68%→7%).
(b) 학습 단에서 spatial cache를 켜서, 지금까지 inference-only 근사였던 sparse spatial 경로를
가중치가 실제로 학습한 경로로 승격(`forward_cached`를 미분 가능 경로로 정리,
`tests/test_gating.py`에서 gradient 흐름 검증). dim 384 + 두 수정 기준 투영:

| active% | GMAC (Δ-gating) | vs 풀연산 | +학습된 cache | vs 풀연산 |
|---|---|---|---|---|
| 100 | 2.844 | 100% | 2.844 | 100% |
| 16.6 | 2.576 | 90.6% | 1.096 | **38.6%** |
| 0 | 2.523 | 88.7% | 0.749 | **26.3%** |

즉 v6로도 "비례"는 아니다 — temporal readout(59%)과 dense embed가 바닥을 만든다. 기여 3의
정직한 형태는 **"시간축 state 경로의 비용은 변화율에 비례하고, 종단 절감은 dense embed/decoder와
공간축에 의해 제한된다"** 이며, 논문에는 이 곡선을 그대로 싣는다.

### 4.12 실촬 고정 카메라 zero-shot (2026-07-26)

주 배포 타깃이 고정 CCTV(§1.2)인데 학습·평가 데이터가 전부 합성 ego-motion이라는 구멍을 메우려
**TUM RGB-D `fr3/sitting_static`**(실촬 Kinect, 카메라 고정, 사람만 움직임 — CCTV 대리
도메인)에서 v3를 zero-shot 평가. 680프레임/85클립, 256px.

로더 결함도 함께 수정: TUM/Bonn은 rgb·depth의 캡처 시각과 프레임 수가 다른데 어댑터가 정렬
순서로 짝지어 GT가 밀려 있었다 → `_pair_by_timestamp`(TUM associate.py와 같은 0.02s 창)로 교체,
`tests/test_data.py`에 회귀 테스트 추가.

| $\tau_{on}$ | active% | AbsRel | RMSE | $\delta_1$ | t-delta | OPW | TCE |
|---|---|---|---|---|---|---|---|
| 0 (풀 연산) | 100.0 | 0.4110 | 1.923 | 0.3661 | 0.1911 | 0.0504 | 0.0580 |
| 0.02 | 32.0 | 0.4020 | 1.867 | 0.3719 | 0.1654 | 0.0443 | 0.0522 |
| 0.05 (config 기본) | **10.3** | 0.3958 | 1.829 | 0.3746 | 0.1480 | 0.0415 | 0.0494 |
| 0.1 | 2.1 | 0.3939 | 1.819 | 0.3754 | 0.1402 | 0.0406 | 0.0485 |
| **const (제어)** | — | **0.2487** | **1.543** | **0.6186** | 0.0000 | 0.0000 | **0.0130** |

**Bonn RGB-D Dynamic 추가 검증** (실촬 실내, 움직이는 사람·물체, 26 시퀀스 29,084 프레임,
1,000 클립, zero-shot) — TUM 결과가 특수 사례가 아님을 확인:

| $\tau_{on}$ | active% | AbsRel | RMSE | $\delta_1$ | t-delta | OPW | TCE |
|---|---|---|---|---|---|---|---|
| 0 (풀 연산) | 100.0 | 0.5704 | 2.789 | 0.4206 | 0.3260 | 0.1324 | 0.1351 |
| 0.05 | 20.1 | 0.5721 | 2.833 | 0.4205 | 0.2980 | 0.1222 | 0.1249 |
| 0.1 | 5.6 | 0.5681 | 2.813 | 0.4216 | 0.2914 | 0.1201 | 0.1229 |
| **const (제어)** | — | **0.2367** | **0.619** | **0.6847** | 0.0000 | 0.0000 | **0.0081** |

같은 패턴: 스킵은 실촬에서도 공짜(active 100→5.6%에 AbsRel 0.5704→0.5681, $\delta_1$ 불변,
시간지표 3종 개선)이지만, **절대 성능은 상수 예측기에 완패**. 두 실촬 데이터셋·1,174/1,000 클립
규모에서 재현되므로 전이 실패는 확정. (Bonn은 핸드헬드 시퀀스가 섞여 active% 20.1%로
TUM static 10.3%보다 높다.) → v7 fine-tune 착수(`configs/main_v7.toml`, 실촬+합성 5소스 혼합,
v3 EMA 가중치에서 lr 1e-4로 15k step).

1. **고정 카메라 active% 주장은 실측으로 입증됨**: $\tau$=0.05에서 active **10.3%**
   (174 클립 확정 수치. 합성 ego-motion 홀드아웃은 같은 $\tau$에서 31.6%) — §1.2가 주장한 "고정 카메라 5–20%"
   구간에 실촬 데이터로 정확히 들어왔다. 스킵이 늘어도 정확도는 오히려 개선
   (AbsRel 0.3877→0.3698, $\delta_1$ 0.378→0.388)되고 세 시간 지표 전부 단조 개선.
2. **그러나 절대 성능은 상수 예측기에 패배**: 클립별 중앙값 상수가 AbsRel 0.2487·$\delta_1$
   0.6186으로 모델(0.3958/0.3746)을 크게 앞서고, 퇴화하지 않는 TCE에서도 0.0130 vs 0.0494로
   모델이 진다. 합성 옥외 위주(vkitti2/tartanair, 깊이 최대 80m급) 학습이 실촬 실내
   Kinect(1.5–8.8m)로 전이되지 않는다. IDEA §3.4의 Stage 1(TIMo 실내 지도학습)/Stage 2(VIRAT)를
   건너뛰고 합성 혼합으로 직행한 결과이며, **현 체크포인트로 CCTV 배포 준비 완료를 주장할 수 없다.**
   대응은 v7(실촬 실내 fine-tune, TUM static + Bonn Dynamic) — PROGRESS.md 참조.

### 4.13 표본 확대 재평가 — §4.7 수치는 표본 편향이었음 (2026-07-26)

§4.7/§4.8의 모든 수치는 홀드아웃 8,929 클립 중 **100개(1.1%)** 표본이고 분산 표기가 없었다.
같은 v3 체크포인트·같은 프로토콜로 **1,000 클립**(결정적 first-N, 모든 모델이 동일 집합)으로
다시 돌린 결과:

| $\tau_{on}$ | active% | AbsRel (std) | RMSE | $\delta_1$ | t-delta | OPW | TCE |
|---|---|---|---|---|---|---|---|
| 0 (풀 연산) | 99.6 | 0.4170 (0.389) | 22.72 | 0.5435 | 0.2866 | 0.0538 | 0.0861 |
| 0.005 | 48.6 | 0.4169 (0.388) | 22.68 | 0.5451 | 0.2573 | 0.0527 | 0.0851 |
| 0.02 | 39.5 | 0.4172 (0.388) | 22.68 | 0.5456 | 0.2505 | 0.0525 | 0.0849 |
| 0.05 (기본) | 31.6 | 0.4174 (0.388) | 22.69 | **0.5457** | 0.2455 | 0.0524 | 0.0847 |
| 0.1 | 24.0 | 0.4182 (0.389) | 22.70 | 0.5454 | 0.2421 | 0.0523 | 0.0846 |
| **const (제어)** | — | 0.7471 (0.334) | 37.88 | 0.2784 | 0.0000 | 0.0000 | **0.0500** |

1. **100 클립 표본은 심하게 편향**되어 있었다: $\delta_1$ 0.397 → **0.5457**, active% 16.6 →
   31.6, AbsRel 0.382 → 0.417. 클립별 AbsRel 표준편차가 0.389(평균 0.417과 같은 규모)로,
   100 표본 평균은 사실상 노이즈였다. **§4.7/§4.8 표는 이 표로 대체**하며, 외부 baseline
   3종도 같은 1,000 클립으로 재실행이 필요하다(진행 중). $\delta_1$ 0.546은 §4.8에서 보고한
   0.394보다 훨씬 높아 DA3(0.562)와 사실상 동급 — "정확도 격차"의 크기 자체가 재산정된다.
2. **핵심 trade-off 패턴은 그대로**: active 99.6→24.0%에서 AbsRel 0.4170→0.4182(상대 +0.3%),
   $\delta_1$ 0.5435→0.5454(오히려 개선), t-delta·OPW·TCE 모두 단조 개선. 스킵 비용이 사실상
   0이라는 결론은 표본을 10배 늘려도 유지된다.
3. **TCE의 const 행은 "GT 자신의 워프 잔차 바닥"으로 읽어야 한다.** 상수 예측의 TCE는 정의상
   $|0 - \Delta D_{gt}|/D_{gt}$, 즉 광학흐름 오차·가림·실제 깊이 변화가 섞인 **데이터셋 고유의
   잔차 수준**(여기서 0.0500)이다. 우리 모델은 0.0847로 이 바닥보다 위에 있다 — 즉 **절대적
   의미의 시간 일관성 우위는 주장할 수 없고**, 외부 baseline 대비 상대 우위만 주장 가능하다.
   (baseline TCE는 재실행 후 확정.)

### 4.14 게이팅 위치 ablation — Δ-gating vs 토큰 drop (2026-07-26)

IDEA §4.4에 있었으나 미실시로 남아있던 항목. "정확한 state 복사"가 실제로 무슨 일을 하는지를
분리하려면, **state는 똑같이 동결하되 readout만 없애는** 대조군이 필요하다. `gate_mode="drop"`
(static 토큰이 temporal 블록을 identity로 우회 — 토큰 pruning/Skip-Conv 계열이 하는 일)을
추가해 같은 detector·같은 마스크·같은 active%로 비교. v3, 홀드아웃 1,000 클립.

| active% | arm | AbsRel | RMSE | $\delta_1$ | t-delta | OPW | TCE |
|---|---|---|---|---|---|---|---|
| 99.6 | Δ-gating | 0.4290 | 26.61 | 0.5260 | 0.2866 | 0.0556 | 0.0892 |
| 99.6 | drop | 0.4826 | 33.49 | 0.4897 | 2.6030 | 0.0950 | 0.1288 |
| 48.6 | Δ-gating | 0.4288 | 26.56 | 0.5276 | 0.2573 | 0.0544 | 0.0882 |
| 48.6 | drop | 1.3698 | 47.60 | 0.1658 | 7.1374 | 0.5337 | 0.5680 |
| 31.6 | Δ-gating | **0.4292** | **26.57** | **0.5285** | **0.2455** | **0.0541** | **0.0879** |
| 31.6 | drop | 1.7178 | 49.50 | 0.1424 | 7.1692 | 0.8252 | 0.8579 |
| 24.0 | Δ-gating | 0.4301 | 26.58 | 0.5282 | 0.2421 | 0.0540 | 0.0878 |
| 24.0 | drop | 1.8727 | 50.16 | 0.1365 | 7.2018 | 0.9285 | 0.9597 |

**해석 — 기여 1의 직접 증거.** 두 arm은 hidden state를 **완전히 동일하게** 동결한다
(`tests/test_gating.py`에서 bit-exact 확인). 차이는 오직 "동결된 state를 현재 프레임의 C로
읽어서 출력에 반영하는가"뿐이다. 그런데 active 31.6%에서 AbsRel 4.0배, $\delta_1$ 0.529→0.142,
t-delta 29배, TCE 9.8배 차이가 난다. 즉 **스킵이 공짜인 이유는 "연산을 건너뛴 것"이 아니라
"보존된 state를 계속 읽는 것"** 이다 — 같은 스킵률에서 readout만 빼면 모델이 붕괴한다.
"변한 것만 재계산" 계열(Eventful Transformer/Skip-Convolutions)이 캐시로 메우는 부분을
SSM state가 대신한다는 NOVELTY 주장이, 이제 대조 실험으로 뒷받침된다.

부수적으로 active 99.6%(사실상 풀연산)에서도 drop이 열화되는 것은, 0.4%의 static 토큰만
우회해도 그 위치의 디코더 입력이 이웃과 이질적이 되어 국소 깊이가 튀기 때문(t-delta 2.60).

세 번째 arm(**출력 캐시만**)은 §3.3에 이미 실측이 있다(저-active에서만 유효, 중간 active%에서
U자형 악화) — 세 arm을 한 표로 합치는 것은 v6에서 캐시가 학습된 경로가 된 뒤에 하는 게 맞다.

### 4.15 외부 SOTA 재비교 — §4.8 헤드라인 주장 부분 기각 (2026-07-26)

§4.8의 비교는 100 클립·t-delta 단일 시간지표·프로토콜 불일치 상태였다. 네 모델 전부
**동일 1,000 클립·픽셀 풀링·동일 워프(OPW/TCE)** 로 재실행:

| 모델 | 파라미터 | AbsRel | RMSE | $\delta_1$ | t-delta | OPW | TCE |
|---|---|---|---|---|---|---|---|
| SOKKANAEM v3 ($\tau$=0.05, active 31.6%) | 2.8M | 0.4292 | 26.57 | 0.5285 | **0.2455** | 0.0541 | 0.0879 |
| SOKKANAEM v4 (distill, $\tau$=0.05) | 2.8M | 0.4315 | 27.24 | 0.5202 | 0.2285 | 0.0510 | 0.0846 |
| Depth Anything V2 Small | 24.8M | 0.5802 | 62.32 | 0.7494 | 9.4742 | 0.2458 | 0.2616 |
| Depth Anything 3 Base | 0.12B | 0.3421 | **17.87** | 0.4848 | 1.8013 | **0.0487** | **0.0603** |
| Video Depth Anything Small (metric) | 28.4M | **0.3274** | 26.99 | **0.7041** | 2.1803 | 0.0590 | 0.0818 |
| const (제어) | — | 0.7434 | 44.75 | 0.2928 | 0.0000 | 0.0000 | 0.0513 |

**살아남은 주장:**

1. **플리커(원시 프레임 간 안정성)는 여전히 압도.** t-delta 0.2455 vs 1.80–9.47 — DA3 대비 7.3배,
   VDA 대비 8.9배, DA v2 대비 38.6배. 명시적 시간 모듈(VDA `motion_module`)이나 8프레임 동시
   처리(DA3)로도 Δ-gating의 정확한 state 복사가 주는 프레임 간 불변성에는 못 미친다.
2. **파라미터 효율.** 2.8M이 24.8M DA v2를 AbsRel(0.429 vs 0.580)·RMSE(26.6 vs 62.3)에서 앞선다.
   DA v2의 클립별 AbsRel std가 2.03으로, scale+shift 정합이 일부 클립에서 폭발하는 것이 원인
   (풀링 수치가 클립평균 0.529보다 높은 이유). $\delta_1$은 0.529 vs 0.749로 뒤진다.
   120M DA3보다 $\delta_1$이 높다(0.529 vs 0.485).

**기각된 주장:**

3. **"시간 안정성은 전부 SOKKANAEM이 압도"는 t-delta의 산물이었다.** 모션 보정된 OPW에서는
   DA3(0.0487)가 우리(0.0541)보다 낫고, GT 기준 TCE에서는 DA3(0.0603)·VDA(0.0818) 둘 다 우리
   (0.0879)보다 낫다. **세 시간지표 중 우리가 1위인 것은 t-delta 하나뿐이며, 나머지 둘에서는
   3위다.** t-delta는 "출력이 프레임 간에 안 움직인다"를 재는 지표이고(상수 출력이 전역 최적),
   OPW/TCE는 "장면 기하가 실제로 움직인 만큼 정확히 따라가는가"를 잰다 — 우리 모델은 앞쪽에서
   강하고 뒤쪽에서 약하다. 논문에서 기여 2는 **"후처리 없는 플리커 제거"** 로 좁혀 서술해야 하며,
   "시간 일관성 전반의 우위"로 확대하면 OPW/TCE 표에서 바로 반박된다.
4. 참고로 우리 TCE(0.0879)는 const 제어행(0.0513, = 데이터셋 워프 잔차 바닥)보다도 위다.
   DA3(0.0603)는 그 바닥에 근접한다.

**v4(DINOv2 distillation) 재평가**: 표본을 10배로 늘리면 v3와 사실상 동일(AbsRel 0.4315 vs
0.4292, $\delta_1$ 0.520 vs 0.529)하나 **시간지표는 일관되게 v4가 낫다**(t-delta 0.229 vs 0.246,
OPW 0.051 vs 0.054, TCE 0.0846 vs 0.0879). §4.9의 "효과 없음" 결론은 정확도에 대해서는 유지,
시간 안정성에 대해서는 소폭 이득으로 수정.

### 4.16 장클립 프로토콜 — §4.5의 "클립 평가가 스킵률을 과소평가" 재현 실패

IDEA §4.5는 스트리밍 실측 active 20–28% vs 클립 평가 68%를 근거로 클립 평가가 스킵률을 심하게
과소평가한다고 했고, 본 학습 평가에 장클립 병기를 요구했다. 32프레임 클립(250개, 홀드아웃)으로 측정:

| $\tau_{on}$ | active% (8프레임) | active% (32프레임) | AbsRel (8f) | AbsRel (32f) |
|---|---|---|---|---|
| 0.005 | 48.6 | 48.9 | 0.4288 | 0.4419 |
| 0.05 | 31.6 | 32.6 | 0.4292 | 0.4429 |
| 0.1 | 24.0 | 25.5 | 0.4301 | 0.4433 |

**차이가 사실상 없다**(active% +1%p, AbsRel +0.3%). 즉 이 데이터 믹스·256px에서는 클립 길이가
스킵률을 좌우하지 않는다 — §4.5의 관찰은 vkitti2 단독·128px·keyframe 리셋 조건에 특수했던 것으로
보이며, 일반화하면 안 된다. 배포 이득이 클립 지표보다 크다는 주장은 근거가 약해졌다
(고정 카메라 실측 active 10.3%(§4.12 갱신)가 그 주장의 더 나은 근거다).

### 4.17 정확도 진단 (2026-07-27) — 용량이 아니라 일반화

절대 정확도를 최소 DA v2 수준까지 올리는 것이 최우선 과제로 지정됨. 학습을 새로 돌리기 전에
병목을 네 가지로 분해해 측정했다.

**(a) 출력 해상도 상한.** 모델은 256px에서 16×16 토큰 그리드 하나로 예측해 16배 업샘플한다.
GT를 같은 병목에 통과시켜(그리드로 average-pool → bilinear 복원 → median-scale) 이 구조가
낼 수 있는 최고점을 측정(200 클립):

| oracle | AbsRel | RMSE | $\delta_1$ |
|---|---|---|---|
| patch 16, depth 공간 (현행 구조) | 0.1746 | 14.16 | 0.8542 |
| patch 16, disparity 공간 | 0.1125 | 17.39 | 0.8791 |
| patch 8, disparity | 0.0737 | 12.71 | 0.9319 |
| patch 4, disparity | 0.0456 | 8.86 | 0.9659 |

우리 모델(0.4166 / 0.5327)은 patch 16 상한(0.1746 / 0.8542)에서 한참 멀다 — **해상도가 아직
병목이 아니다.** 단 DA v2($\delta_1$ 0.749)를 넘으려면 상한의 88%를 뽑아야 하므로 여유가 없고,
patch 8로 내려가면 상한이 0.932로 올라간다(토큰 4배 = SSM 비용 4배). disparity 공간 풀링이
AbsRel을 0.175→0.113으로 낮추는 것은 far-field가 역수 공간에서 훨씬 잘 보존된다는 뜻이다.

**(b) 학습 split vs 홀드아웃.** v7을 학습에 쓴 클립에서 채점(300 클립씩, stride 97):

| split | AbsRel | RMSE | $\delta_1$ |
|---|---|---|---|
| TRAIN (학습에 사용) | **0.1918** | 25.47 | **0.7074** |
| HOLDOUT (미학습) | 0.3562 | 15.66 | 0.5185 |

학습 데이터에서는 AbsRel 0.192로 patch 16 상한(0.175)에 근접하고 $\delta_1$ 0.707로 **DA v2의
홀드아웃 $\delta_1$ 0.749에 근접**한다. 즉 모델 용량·최적화·손실은 이미 충분하고, **문제는
미학습 장면으로의 전이**다.

**(c) 소스별 분해.**

| 소스 | split | AbsRel | $\delta_1$ |
|---|---|---|---|
| vkitti2 | train / holdout | 0.2333 / 0.2670 | 0.5701 / **0.6321** |
| tartanair2 | train / holdout | 0.3968 / 0.7246 | 0.4821 / 0.3967 |
| pointodyssey | train / holdout | 0.1881 / 0.2819 | 0.8300 / 0.5603 |

vkitti2는 홀드아웃이 학습보다 오히려 좋다(과적합 아님). tartanair2는 **학습 데이터에서조차
최악**(0.397/0.482)이고 홀드아웃(단일 미학습 env)에서 더 나빠진다. pointodyssey가 가장 큰
전이 격차를 만든다.

**(d) GT 무결성 (tartanair2 의심 검증).** 학습에서도 안 맞으니 타깃 손상을 의심해 확인:
73개 시퀀스 전부 rgb/depth 프레임 수 일치(정렬순 페어링이 안전), DA v2 disparity와 1/GT의
상관계수 vkitti2 0.994 / pointodyssey 0.945 / bonn 0.957 / **tartanair 0.831**. 손상은 아니고,
tartanair2가 한 프레임에서 0.5–129m를 담는 초광역 범위라서 어렵다는 것(중앙값 2.0m, p95 128.5m).
즉 **depth 공간 회귀가 이 소스에 특히 불리**하다 — (a)의 disparity 결과와 같은 방향.

**대응 — 기존 고정밀 깊이 모델 관행 3종 이식** (전부 추론 비용 0 또는 감소):

1. **클립 일관 데이터 증강** (`ClipDataset(augment=True)`): 랜덤 리사이즈 크롭(zoom 0.55–1.0)·
   좌우 반전·밝기/대비/채도/감마 지터. 클립당 1회 추첨해 모든 프레임에 동일 적용 —
   시간 구조와 마스크·temporal loss 유효성 유지. **기존 파이프라인에는 증강이 전혀 없었다**
   (resize + center crop만). (b)의 진단이 가리키는 직접적 대응.
2. **DPT식 다중스케일 융합 디코더** (`DPTDecoder`): 기존 디코더는 16×16 특징 하나를 skip 없이
   업샘플해, 네트워크가 패치보다 고운 디테일을 아예 못 본다. 대신 (i) 모든 백본 블록의 토큰을
   1×1 projection으로 합산, (ii) RGB에 stride-2 conv stem 3층을 얹어 1/2·1/4·1/8 디테일을
   skip으로 공급, (iii) 1/16→1/8→1/4→1/2 단계 융합 후 마지막 2배만 bilinear.
   채널을 해상도에 반비례하게 테이퍼(1/2에서 16채널) — 풀해상도 64채널 conv 한 층이 2.4 GMAC로
   모델 전체보다 비싸기 때문. **파라미터는 오히려 감소(0.31M→0.13M), 속도 동일(6.2ms/step).**
3. **disparity 회귀 + MiDaS 다중스케일 gradient matching 손실**
   (`losses.multiscale_grad_loss`): 정규화 disparity(중앙값·MAD 정규화) 위에서 4단 해상도
   피라미드로 gradient를 맞춘다. 단일 스케일 gradient는 1픽셀 경계만 보고, 피라미드는 저주파
   형상 오차까지 벌점 — 고정밀 모델이 블러 대신 선명하게 나오는 이유.

검증: 테스트 43개 통과(증강의 클립 일관성·rgb/depth 기하 정합, 피라미드 손실이 블러를 벌점,
DPT 디코더의 스트리밍 계약·Δ-gating 불변 유지 포함). 128px·15k step 프로브 3종
(`scripts/probe_accuracy.sh`: 무증강 / 증강 / 증강+DPT+피라미드)으로 무엇이 실제로 듣는지
분리 측정 후 256px 장기 학습 착수. **v5(용량 증가)·v6(효율)는 이 작업 뒤로 밀림.**

### 4.18 v8 — Mamba-depth 정밀화 기법 이식 + 3-arm 처치 분리 (2026-07-27~28)

Mamba 기반 깊이 추정 계열의 고정밀화 기법 4종을 **희소 경로 안에만** 배치해 이식했다
(SS2D 4방향 cross-scan, depthwise 3x3 국소 분기, DPT 다중스케일 융합 디코더, 64-bin
classification-regression 헤드). 추가로 `temporal_cache`(static 토큰의 readout까지 재사용)와
teacher 출력 증류를 넣었다. 상세 배치 근거는 `configs/main_v8.toml` 헤더.

**FLOPs 회계** (256px, `scripts/flops.py --decoder dpt --scan-directions 4 --local-conv --bins 64`):

| | v7 구조 | v8 |
|---|---|---|
| full compute | 2.337 GMAC | 1.644 GMAC |
| dense 몫 (embed+decoder) | 69.4% | 25.4% |
| active%에 비례하는 몫 | 21.8% | 62.0% |
| active 16.6% (캐시 전부) | 1.841 (78.8%) | 0.623 (37.9%) |
| active 0% | 1.743 (74.6%) | 0.420 (25.5%) |

즉 정밀화 모듈을 넣으면서 full compute가 30% 싸졌고 절감 곡선이 ideal에 2배 가까워졌다.
남은 바닥 25.5%는 dense embed(2.3%) + DPT 디코더(23.1%).

**v8 학습**: 60k step, v7 EMA 부분 승계(`--resume-partial`, 신규 텐서 84/미사용 8), 12h58m.

**처치 분리 3-arm** (각 v8 가중치에서 8k step, lr 1e-4, 동일 데이터·평가):

| 합성 500 클립 (active 15.4%) | AbsRel | $\delta_1$ | RMSE | t-delta | TCE |
|---|---|---|---|---|---|
| v7 | 0.2895 | **0.5923** | **31.77** | **0.1591** | **0.0251** |
| v8 (60k) | 0.3366 | 0.4151 | 38.37 | 0.1828 | 0.0273 |
| arm0 = v8 +8k (처치 없음) | 0.3373 | 0.4155 | 38.12 | 0.1911 | 0.0275 |
| arm1 = teacher 제거 | 0.2867 | 0.5629 | 38.82 | 0.1711 | 0.0263 |
| **arm2 = teacher 제거 + bin CE** | **0.2735** | 0.5898 | 38.49 | 0.2010 | 0.0273 |

| 실촬 미학습 487 클립 (active 17.9%) | AbsRel | $\delta_1$ | t-delta | TCE |
|---|---|---|---|---|
| v7 | 0.179 | 0.790 | — | — |
| v8 (60k) | 0.1642 | 0.8111 | 0.0721 | 0.0261 |
| arm0 | 0.1600 | 0.8156 | 0.0740 | 0.0259 |
| arm1 | 0.1482 | 0.8405 | **0.0410** | **0.0220** |
| **arm2** | **0.1386** | **0.8472** | 0.0426 | 0.0240 |

판정:

1. **teacher 출력 증류(DA v2)는 순손실** — 끄기만 해서 실촬 $\delta_1$ +2.5pt, 합성 +14.7pt.
   arm0이 v8과 사실상 동일하므로 추가 학습 효과가 아니라 처치 효과다. affine-invariant로
   상대 disparity를 맞추게 하면 TartanAir의 0.5–129 m 원거리 구조가 끌려 나간다.
   **v8의 실촬 개선을 teacher 공로로 본 최초 귀속은 오류** — 구조 변경이 올린 것을 teacher가
   깎고 있었다. `configs/main_v8.toml`에서 제거.
2. **bin 감독 손실은 유효** (`losses.bin_ce_loss`, `--bin-weight`). 실촬 AbsRel −6.5%,
   합성 $\delta_1$ +2.7pt. 대가로 t-delta 0.1711→0.2010 후퇴.
3. **binning은 감독 없이는 작동하지 않았다** (실촬 120 클립 분포 진단):

| | v8 (감독 없음) | arm2 (bin CE) |
|---|---|---|
| 정규화 엔트로피 (경계 / 평탄) | 0.776 / 0.773 | 0.408 / 0.354 |
| 분포 σ, log-depth (경계 / 평탄) | 1.027 / 1.017 | 0.296 / 0.229 |
| 최빈 bin 질량 < 50% 픽셀 | 99.9% | 58.4% |
| AbsRel (expectation / mode / top3) | 0.2511 / 0.2888 / 0.2836 | 0.1961 / 0.2203 / 0.2094 |

   감독 전에는 **경계와 평탄이 구별되지 않았다**(0.776 vs 0.773) — 기대값만 맞추면 되는
   최적화에서 넓은 분포가 정당한 해이므로, bins=64는 softmax를 통과한 스칼라 회귀였다.
   감독 후 경계가 평탄보다 넓어지는 관계가 처음 성립. **bins를 기여로 주장하려면 이 표가
   근거이고, 감독 항이 필수 구성요소다.**

**범위 제한**: arm2가 v7을 전면적으로 이기지 않는다. 실촬은 명확한 우위(AbsRel 0.1386 vs
0.179)지만 합성에서는 $\delta_1$ 사실상 동일(−0.25pt), **RMSE 21% 열위**(38.49 vs 31.77),
t-delta·TCE도 열위. RMSE는 원거리 오차에 민감하므로 log-depth bin 상단(145 m까지 64 bin)의
해상도 부족이 유력 후보. "전면 우위"로 쓰면 즉시 반박된다.

**정성 결과** (`outputs/v8/`, `outputs/arm2-binloss/`, 각 10장, RGB | 예측 | GT):
전역 배치·깊이 순서는 맞지만 전경 물체(사람·기둥·의자)가 예측에서 살아나지 않는다. 원인은
그리드 상한이 **아니다** — 예측의 평균 |∇log depth| 0.0057은 16px 오라클 0.0063과 거의 같고
(GT 0.0085), 같은 클립에서 16px 오라클 AbsRel 0.0794 / $\delta_1$ 0.9227, 32px 오라클
0.1459 / 0.8141 대비 모델은 0.2511 / 0.7446(v8) — **32px 블록 상수보다 나쁘다**. 고주파가
없어서가 아니라 값이 틀려서 GT와 겹치지 않는 것이고, 남은 격차는 표현 품질 쪽이다.

### 4.19 평가 프로토콜 정정 + 재현성 배관 + wall-clock 도구 (2026-07-29)

`reports/20260729.md` 감사의 P0/P2 항목을 코드에 반영했다.

**(a) 소스별 평가로 전환 — "합성 500클립"은 VKITTI2 500클립이었다.**
`scripts/eval.py`가 `ConcatDataset`을 순서대로 읽고 `--max-clips`에서 끊었기 때문에,
first-N 표본이 첫 데이터셋만 담았다. 이제 소스마다 loader를 만들고 `--max-clips`는
**소스당** 개수이며, 통합행을 두 개로 분리한다. `MEAN(src)`는 데이터셋 균등 평균,
`POOLED(px)`는 전체 픽셀 가중이다.

**(b) active ratio가 소스마다 4배 이상 다르다** — 효율 주장의 기준점이 바뀐다.

| 소스 | tum | bonn | vkitti2 | pointodyssey | tartanair2 |
|---|---:|---:|---:|---:|---:|
| active @ tau 0.05 | 14.5% | 29.9% | 16.6% | 24.0% | 70.3% |

기존 "합성 15.4%"는 VKITTI2 단일 소스 값이었다. 데이터셋 균등 기준 합성 평균은 **37.0%**,
실촬 평균은 **22.2%**다. 이를 두 cache 사용 시 해석적 MAC(`scripts/flops.py`, v8 구조,
full 1.644 GMAC)에 대입하면:

| 소스 | tum | vkitti2 | pointodyssey | bonn | tartanair2 | 실촬 평균 | 합성 평균 |
|---|---:|---:|---:|---:|---:|---:|---:|
| GMAC | 0.597 | 0.623 | 0.714 | 0.786 | 1.280 | 0.692 | 0.873 |
| 풀연산 대비 | 36.3% | 37.9% | 43.4% | 47.8% | **77.9%** | **42.1%** | **53.1%** |

즉 "37–39%"는 가장 정적인 소스의 값이다. 이동량이 큰 TartanAir에서는 78%로 희소 경로의
이득이 거의 사라진다 — **효율 주장은 항상 장면 변화율과 함께 보고해야 한다.**

**(c) 같은 프로토콜로 v7 vs arm2 재측정** (소스당 100클립, 256px, tau 0.05, clip_len 8).
`scale`은 clip median 정합 배율(1이면 절대 스케일 일치), `drift`는 클립 내 프레임별
배율의 변동계수, `mAbsRel`은 median scaling **없는** 절대 오차다.

| 도메인 | 모델 | AbsRel ↓ | RMSE ↓ | δ1 ↑ | t-delta ↓ | OPW ↓ | TCE ↓ | mAbsRel ↓ | scale | drift ↓ |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 실촬 MEAN(src) | v7 | 0.2362 | 0.7345 | 0.7568 | **0.0655** | 0.0256 | 0.0330 | 0.2477 | 1.182 | 0.0240 |
| 실촬 MEAN(src) | **arm2** | **0.1760** | **0.6578** | **0.8027** | 0.0690 | **0.0242** | **0.0324** | **0.1859** | **1.053** | **0.0249** |
| 합성 MEAN(src) | v7 | 0.4141 | **14.60** | 0.4520 | 0.2064 | 0.0467 | 0.0766 | 0.4716 | 1.885 | 0.0803 |
| 합성 MEAN(src) | **arm2** | **0.3810** | 15.69 | **0.4580** | **0.1971** | **0.0353** | **0.0663** | **0.4410** | **1.380** | **0.0824** |

소스별로 보면 arm2는 tum 0.1304 / bonn 0.2217, vkitti2 0.3676 / pointodyssey 0.2684 /
tartanair2 0.5069이다. **§4.18의 "합성에서 시간 안정성이 후퇴했다"는 판정은 표본 편향이었다**
— VKITTI2 단독으로는 t-delta·TCE가 v7보다 나쁘지만, 세 합성 소스를 균등 평균하면 arm2가
t-delta·OPW·TCE 모두 앞선다. 반면 **RMSE 열위는 균등 기준에서도 남는다**(15.69 vs 14.60).

절대 스케일: arm2는 median 정합 배율이 실촬 1.053, 합성 1.380으로 v7(1.182 / 1.885)보다
1에 가깝다. 즉 상대 구조뿐 아니라 metric scale도 개선됐지만, 합성 원거리는 여전히 38%
과소추정이다(TartanAir 배율 1.798). 고정 CCTV에서 미터 단위가 필요하면 이 숫자가 기준이다.

주의: 이 표는 소스당 앞 100클립 표본이라 §4.18의 487/500클립 표와 **직접 비교 불가**하다.
v7·arm2를 같은 프로토콜로 다시 돌렸으므로 모델 간 비교만 유효하다.

**(d) 재현성 배관.** `train.py`가 이제 (i) `--seed`로 torch/random을 고정하고,
(ii) work dir의 `config.toml`을 입력 TOML 복사본이 아니라 **실효 설정**(CLI+TOML 병합값 +
`[model]` + `[meta]` git commit·torch/CUDA 버전·GPU·실행 명령)으로 쓰고, (iii) 같은 메타를
체크포인트에도 함께 저장한다. 기존 `arm1/arm2/config.toml`이 실제 실행과 달리
`teacher_weight = 0.5`를 남겨 두던 문제는 해당 파일에 정정 주석과 함께 수정했다.

**(e) 기본 추론이 희소 경로를 껐던 문제.** `infer.py --spatial-cache`가 store_true라서
기본값 False가 체크포인트의 `spatial_cache=true`를 덮어썼고, `--size` 기본값 128이 256px
학습 모델을 128px로 돌렸다. 두 플래그를 tri-state(`--no-spatial-cache`로만 끔)로 바꾸고
해상도를 sidecar config에서 복원한다. 기본 실행이 이제 `size 256 spatial_cache True
temporal_cache True`를 출력한다. `eval.py`도 동일하게 수정.

**(f) `scripts/bench.py` 신설 + 확정 wall-clock** — active ratio별 latency/FPS,
cache on/off, fp16, 다중 스트림, peak VRAM, 스트림당 영속 state를 실측한다. active ratio는
detector 대신 고정 비율 stub으로 강제해 x축을 정확히 만들고, 각 지점을 3회 측정해 최솟값을
쓴다(다른 작업이 GPU에 있으면 느려지기만 하므로 — 실제로 오염된 측정에서 같은 dense 경로가
13 ms와 47 ms로 튀었다). 아래는 **GPU 단독 점유** 상태에서 `v8-teacherfree-60k`로 측정한
값(256px, batch 1, RTX 4090).

| active | fp32 두 cache | fp32 cache off | fp16 두 cache | fp16 cache off |
|---:|---:|---:|---:|---:|
| 0% | **0.64 ms / 1571 FPS** | 11.58 ms | 0.68 ms / 1474 FPS | 12.07 ms |
| 14.9% | 4.27 ms / 234 FPS | 11.50 ms | 4.50 ms / 222 FPS | 12.07 ms |
| 22.2% (실촬 평균) | 4.94 ms / 203 FPS | 11.67 ms | 5.19 ms / 193 FPS | 12.03 ms |
| 37.0% (합성 평균) | 6.44 ms / 155 FPS | 11.52 ms | 7.18 ms / 139 FPS | 12.12 ms |
| 100% | 11.89 ms / 84 FPS | 11.38 ms | 12.10 ms | 11.98 ms |
| peak VRAM | 87.8 MB | 87.0 MB | 48.0 MB | 47.6 MB |
| 스트림당 state | 12.75 MB | 12.00 MB | 6.38 MB | 6.00 MB |

읽을 점 네 가지.

1. **dense 경로는 active와 무관하게 평평**(11.4–11.7 ms)하고 희소 경로만 변화율을 따라간다.
   실촬 평균 22.2%에서 11.67 → 4.94 ms, **2.36배**다. 해석적 MAC 예측(42.1% = 2.38배)과
   사실상 일치한다 — **§4.11의 "launch-bound라 MAC 절감이 FPS로 이어지지 않는다"는 판정은
   v8 구조에서 뒤집혔다.** DPT decoder로 dense 바닥을 낮추고 두 cache를 넣은 결과다.
2. **fp16은 속도 이득이 없다**(12.07 vs 11.58 ms) — 여전히 launch-bound라는 증거다. 대신
   peak VRAM과 스트림 state가 정확히 반감한다(87→48 MB, 12.75→6.38 MB/stream). 스트림당
   state 실측 12.75 MB는 §6.4 이론값 14.16 MB와 부합한다.
3. **`--compile`(CUDA graph)은 dense 경로를 fp32 11.58 → 4.67 ms(214 FPS),
   fp16 → 3.81 ms(263 FPS)로 줄인다.** 즉 dense 시간의 약 60%가 커널 런치 오버헤드였다.
   희소 경로는 동적 shape이라 아직 compile 불가이므로 **현재 희소 이득은 "eager 대비"이고,
   compiled dense(4.67 ms)와 비교하면 실촬 평균에서 거의 동률(4.94 ms)이다.** Triton 블록
   희소 커널이 필요한 이유가 이 한 줄이다 — 그 전까지 희소 경로의 실질 우위는 저활동 구간
   (active < 15%, 0%에서 18배)에 있다.
4. **멀티스트림에서 효율이 붙는다**: fp16 batch 4, active 15.1%에서 14.2 ms/frame =
   **281 FPS**(같은 조건 dense 200 FPS), peak VRAM 98.6 MB. 고정 CCTV 다중 채널이 타깃이면
   배치가 런치 오버헤드를 분담하는 이 구간이 실제 운용점이다.

**(g) teacher-free 60k 재학습 완주** (`work_dirs/v8-teacherfree-60k`, 14h31m, seed 0,
v7 EMA partial init, teacher 없음 + bin CE). arm2의 "8k 회복 학습" 한계를 없애기 위한 P1
실행이었고, 같은 소스별 프로토콜 결과는:

| 도메인 | 지표 | v7 | arm2 (8k 회복) | **60k 정식** |
|---|---|---:|---:|---:|
| 실촬 MEAN(src) | AbsRel ↓ | 0.2362 | 0.1760 | **0.1685** |
| 실촬 MEAN(src) | δ1 ↑ | 0.7568 | 0.8027 | **0.8083** |
| 실촬 MEAN(src) | mAbsRel ↓ | 0.2477 | 0.1859 | **0.1743** |
| 실촬 MEAN(src) | TCE ↓ | 0.0330 | **0.0324** | 0.0354 |
| 합성 MEAN(src) | AbsRel ↓ | 0.4141 | **0.3810** | 0.3842 |
| 합성 MEAN(src) | δ1 ↑ | 0.4520 | 0.4580 | **0.4656** |
| 합성 MEAN(src) | RMSE ↓ | **14.60** | 15.69 | 15.20 |
| 합성 MEAN(src) | TCE ↓ | 0.0766 | **0.0663** | 0.0710 |

정확도는 소폭 우위, 시간 안정성은 소폭 열위다. 중요한 것은 순위가 아니라 **arm2의 수치가
회복 학습 아티팩트가 아니라 레시피 자체의 성질임이 재현으로 확인된 것**이다(감사 §10 최우선
리스크 2 해소). 반대로 **합성 RMSE 열위는 세 체크포인트 모두에 남는다** — 원거리 bin 해상도
가설은 여전히 유효하고, 이건 학습 레시피가 아니라 head 설계 쪽 문제다.

**(h) 배포 경로 버그 2건** — 둘 다 이번 계측에서 처음 드러났다.

1. **fp16 추론이 아예 불가능했다.** detector는 모델 dtype과 무관하게 fp32 mask를 만들고,
   Δ-gating의 `dt * mask`가 스캔 전체를 fp32로 승격시켜 다음 einsum이
   `expected scalar type Float but found Half`로 죽었다. `ssm.py`에서 mask를 dt dtype으로
   캐스팅해 모든 호출자를 한 번에 고쳤다(`gate_mode="drop"` 경로도 동일).
2. **4방향 scan + `--compile`이 CUDA graph에서 죽었다.** cross-scan 순열 텐서가 캡처된 영역
   안에서 처음 생성돼 cudagraph pool에 들어가고, 다음 replay가 그 버퍼를 덮어썼다. 순열
   캐시를 graph 밖에서 채우도록 수정.

체크포인트 저장에도 같은 종류의 함정이 있었다: `[meta]`의 `torch.__version__`은
`TorchVersion`(str **서브클래스**)이라 torch 2.6+의 `weights_only=True` 기본값이 로드를
거부했다 — 14시간 학습이 끝난 뒤에야 드러나는 실패라 회귀 테스트를 붙였다
(`tests/test_repro.py`, `tests/test_arch.py::test_half_precision_streaming`).

### 4.20 Tier 0 — 고활동 dense 폴백, seed 분산 분리, baseline 재생성 (2026-07-30)

[PLAN.md](PLAN.md) Tier 0을 하나의 detached 큐로 실행(`work_dirs/tier0.sh`, 로그
`work_dirs/tier0-console.log`, 03:47–14:20). 대상 체크포인트는 §4.19g의 60k teacher-free.

**(a) T0-3 고활동 dense 폴백 — 채택.** detector active가 `dense_above`(기본 0.4)를 넘으면 그
프레임은 dense 경로로 돌리고 두 캐시를 전부 갱신하며, `active_ratio`도 1.0으로 보고한다(지불한
연산을 정직하게 적는다). 같은 체크포인트·같은 프로토콜 비교:

| | active% | AbsRel ↓ | RMSE ↓ | δ1 ↑ | t-delta ↓ | OPW ↓ | TCE ↓ |
|---|---:|---:|---:|---:|---:|---:|---:|
| 실촬 MEAN(src), 폴백 off | 22.2 | 0.1685 | 0.6445 | 0.8083 | **0.0881** | 0.0274 | 0.0354 |
| 실촬 MEAN(src), 폴백 on | 32.2 | **0.1633** | **0.6279** | **0.8211** | 0.0915 | **0.0271** | **0.0351** |
| 합성 MEAN(src), 폴백 off | 37.0 | 0.3842 | 15.20 | **0.4656** | **0.2018** | — | **0.0710** |
| 합성 MEAN(src), 폴백 on | 49.6 | **0.3826** | 15.18 | 0.4651 | 0.2239 | 0.0452 | 0.0759 |

이득은 **실촬에 몰려 있고 Bonn이 전부**다(Bonn AbsRel 0.2054→0.1927, δ1 0.7807→0.8043,
active 29.9→44.7). TUM은 중립(AbsRel 0.1316→0.1338, δ1 0.8359→0.8379), 합성은 정확도 동결에
연산만 +12.6pt다 — TartanAir는 active 70.3→92.1로 사실상 항상 dense가 된다. 즉 이 정책은
"연산을 더 쓰고 실촬 정확도를 산다"이지 공짜가 아니며, **속도 주장을 이제 실촬 active 22%가
아니라 32%에서 인용해야 한다**. 기본값을 0.4로 유지하는 근거는 배포 대상 도메인이 실촬이고
거기서 AbsRel·RMSE·δ1·OPW·TCE가 전부 개선되기 때문이다(t-delta만 3.9% 열화). 캐시 자체를
검증하는 테스트는 `dense_above=0`으로 정책을 끈다.

부수적으로 §3.3의 "U자형 악화" 서사는 **더 이상 이 모델의 근거가 아니다**: 그건 캐시가
추론 전용이던 v6 측정이고, 지금은 캐시 경로가 학습에 포함돼 고활동 구간에서도 정확도가
붕괴하지 않는다(합성 정확도 동결이 그 증거). 폴백의 실제 효용은 U자 회피가 아니라 Bonn 같은
**중활동 실촬에서 stale context를 끊는 것**이다.

**(b) T0-2 seed 분산 분리 — bin CE 효과는 분산 밖.** v8 init에서 8k step, seed 0/1/2 ×
bin-weight {0.2, 0.0} 6회(`work_dirs/t0-seed*-bin*`).

| | bin CE 0.2 (3 seed) | bin CE 0.0 (3 seed) | 차이 |
|---|---|---|---|
| 실촬 AbsRel | 0.1795 ± 0.0026 | 0.1963 ± 0.0050 | **−8.6%** |
| 실촬 δ1 | 0.8074 ± 0.0008 | 0.7931 ± 0.0043 | **+1.43pt** |
| 실촬 t-delta | 0.0746 ± 0.0049 | 0.0689 ± 0.0049 | +8.4% (열화) |
| 실촬 TCE | 0.0336 ± 0.0015 | 0.0300 ± 0.0002 | +12% (열화) |
| 합성 AbsRel | 0.3769 ± 0.0019 | 0.3795 ± 0.0044 | 분산 내 |
| 합성 δ1 | 0.4709 ± 0.0153 | 0.4752 ± 0.0159 | 분산 내 |

bin CE의 실촬 이득은 seed 분산의 3~6배로 **재현된다**(§4.18 결론 유지). 새로 드러난 것 둘:
bin CE는 정확도를 사고 **시간 안정성을 판다**(t-delta +8%, TCE +12%) — T1-6이 다뤄야 할
trade-off다. 그리고 **합성 δ1의 seed 표준편차가 ±0.015**라 3pt 미만의 합성 δ1 차이는 전부
노이즈다(실촬 δ1은 ±0.004로 훨씬 안정). 과거 보고서의 합성 δ1 소차 비교는 이 기준으로
재해석해야 한다.

**(c) T0-1 baseline 재생성 — DA3는 5소스 완료, DA v2는 환경 결손.** 소스별 100클립, 우리와
동일 holdout·프로토콜. DA3-Base(0.12B) pooled:

| 소스 | AbsRel ↓ | RMSE ↓ | δ1 ↑ | t-delta ↓ | OPW ↓ | TCE ↓ |
|---|---:|---:|---:|---:|---:|---:|
| vkitti2 | 0.2707 | 17.72 | 0.5663 | 2.0443 | 0.0297 | 0.0161 |
| tartanair2 | 0.4539 | 2.368 | 0.3713 | 0.4556 | 0.0666 | 0.1164 |
| pointodyssey | 0.3608 | 4.082 | 0.3850 | 0.5384 | 0.0484 | 0.0455 |
| tum | 0.1816 | 1.166 | 0.8038 | 0.1237 | 0.0180 | 0.0278 |
| bonn | 0.0671 | 0.234 | 0.9541 | 0.0811 | 0.0189 | 0.0226 |

DA v2 Small(24.8M, 상대 깊이 — 클립당 disparity scale+shift 정합) pooled:

| 소스 | AbsRel ↓ | RMSE ↓ | δ1 ↑ | t-delta ↓ | OPW ↓ | TCE ↓ |
|---|---:|---:|---:|---:|---:|---:|
| vkitti2 | 0.1220 | 63.72 | 0.8992 | 7.9063 | 0.0431 | 0.0359 |
| tartanair2 | 0.9145 | 62.91 | 0.4195 | 6.5050 | 0.2380 | 0.2911 |
| pointodyssey | 0.1088 | 4.542 | 0.8717 | 0.6259 | 0.0500 | 0.0525 |
| tum | 0.3557 | 24.31 | 0.8828 | 1.8697 | 0.1598 | 0.1679 |
| bonn | 0.0954 | 0.3425 | 0.9271 | 0.1142 | 0.0317 | 0.0350 |

**동급 파라미터(24.8M) 대비로는 우리가 이긴다.** 실촬 MEAN(src): AbsRel 0.1633 vs 0.2256,
RMSE 0.628 vs 12.3, TCE 0.0351 vs 0.1015, t-delta 0.0915 vs 0.9920 — δ1만 0.8211 vs 0.9050으로
열위다. DA v2의 AbsRel·RMSE가 δ1과 이렇게 어긋나는 이유는 상대 깊이 모델이라 클립당 shift를
같이 맞추기 때문으로, 순위 픽셀 대부분은 맞지만(δ1 높음) 원거리 몇 %가 폭주해 제곱 오차를
지배한다(TUM RMSE 24.3 m). 즉 **동급 크기 비교는 우리 쪽 프레이밍(PLAN "고치지 않고 프레이밍")
을 실제로 지지한다**.

MEAN(src)로 우리(60k, 폴백 on) 대비: 실촬은 DA3 0.1244/0.8790/0.0252(AbsRel/δ1/TCE) vs 우리
0.1633/0.8211/0.0351 — **정확도·TCE 모두 열위**, t-delta만 0.0915 vs 0.1024로 우위. 합성은
DA3 0.3618/0.4409/0.0593 vs 우리 0.3826/0.4651/0.0759 — δ1 우위, AbsRel·TCE 열위,
t-delta는 0.2239 vs 1.0128로 4.5배 우위. §4.15의 그림(정확도 열위 + 원시 플리커 우위)이
최신 체크포인트와 소스별 프로토콜에서도 유지된다. DA v2는 `baselines` env에서
`transformers`가 사라져 실패했고 `sokkanaem` env(5.14.1)로 재실행했다(`work_dirs/t0-da2.log`).
VDA는 로컬 체크아웃 소실로 이번 라운드 제외 — §4.15 수치를 계속 인용한다.

**(d) T0-4 배율 드리프트 — 클립이 길어지면 드리프트가 비례 이상으로 커진다.** 8프레임 대비
32프레임(30클립/소스):

| | 실촬 drift | 합성 drift | 실촬 AbsRel | 합성 AbsRel |
|---|---:|---:|---:|---:|
| 8프레임 | 0.0235 | 0.0934 | 0.1633 | 0.3826 |
| 32프레임 | 0.0444 (×1.9) | 0.1587 (×1.7) | 0.1793 | 0.4178 |

프레임 수 4배에 클립 내 배율 변동은 1.7~1.9배 — 랜덤워크(√4 = 2배)보다 약간 낮아 **체계적
드리프트가 아니라 확산에 가깝다**. 다만 TartanAir는 drift 0.1986→0.3169(×1.6)에 배율 자체가
1.98로 GT의 2배라, 이 소스에서는 median scaling 없이는 아무 의미가 없다. metric scale 주장은
계속 하지 않는다.

### 4.21 Tier 1 — 손실 2종 신설, bin 범위 가설 기각 (2026-07-30~31)

`work_dirs/tier1.sh`, 6 arm × 8k step, 모두 같은 v8 init·seed 0. 대조군은 완전히 같은 명령의
`work_dirs/t0-seed0-bin0.2`. 판정 기준은 §4.20b의 seed 노이즈(실촬 AbsRel ±0.005,
실촬 δ1 ±0.004, 합성 δ1 ±0.015).

| arm | 실촬 AbsRel ↓ | 실촬 δ1 ↑ | 실촬 TCE ↓ | 실촬 OPW ↓ | 합성 RMSE ↓ | 합성 t-delta ↓ | 합성 TCE ↓ |
|---|---:|---:|---:|---:|---:|---:|---:|
| 대조군 | 0.1773 | 0.8082 | 0.0344 | 0.0263 | 15.53 | 0.2016 | 0.0664 |
| `d_max` 600 | 0.1768 | 0.8095 | 0.0338 | 0.0257 | 15.37 | 0.2134 | 0.0662 |
| bins 128 + `d_max` 600 | 0.1842 | 0.8044 | 0.0338 | — | 15.51 | 0.2212 | 0.0702 |
| warp 0.5 | 0.1808 | 0.8071 | 0.0327 | 0.0245 | 15.61 | 0.1843 | 0.0631 |
| warp 2.0 | 0.1857 | 0.7987 | **0.0304** | **0.0220** | 15.65 | **0.1491** | **0.0560** |
| edge 0.5 | 0.1761 | 0.8107 | 0.0348 | 0.0268 | 14.99 | 0.2283 | 0.0673 |
| edge 2.0 | **0.1745** | **0.8107** | 0.0349 | 0.0269 | **14.51** | 0.2667 | 0.0673 |

**(a) T1-5 bin 범위 가설 기각.** `d_max` 150→600은 어느 지표도 seed 노이즈를 넘지 못했다
(합성 RMSE 15.53→15.37). bin_probe의 산술(§4.20 (c) 아래)은 맞다 — 115 m 상한을 넘는
픽셀이 제곱오차의 절반을 낸다 — 그러나 **상한을 풀어줘도 모델이 그 범위를 쓰지 않는다**.
8k step 부족일 수도 있으나 128 bin은 오히려 양쪽 도메인에서 나빠졌으므로, 원거리 열위는
head 표현력이 아니라 **256px 입력에서 300~600 m 구조를 볼 수 없다는 정보 한계**로 보는 것이
현재 증거에 맞다. `d_max`는 kwarg로 남기되 기본값 150을 유지한다.

**(b) T1-6 warp residual 손실 — 채택 대상, 용량은 정확도와의 교환.** 가중치를 올릴수록 시간
지표가 단조 개선되고 실촬 정확도가 단조 악화한다: 0.5에서 TCE −5%/AbsRel +0.0035(노이즈 내),
2.0에서 TCE −12%·OPW −16%·합성 t-delta −26%인 대신 실촬 δ1이 −0.0095로 노이즈 밖으로
나간다. §4.20b가 드러낸 "bin CE가 시간 안정성을 판다"를 정확히 되사는 항이다. 합성에서는
교환이 아니라 순이득이다(AbsRel·δ1도 함께 개선).

**(c) T1-7 edge 가중 손실 — 공짜 이득, 다만 목표가 빗나갔다.** edge 2.0은 실촬 AbsRel
−1.6%·δ1 +0.25pt에 **합성 RMSE 15.53→14.51**로, T1-5가 bin 재설계로 노리던 목표치(14.60)를
bin을 건드리지 않고 넘겼다. 원거리 오차의 상당 부분이 깊이 불연속 근처에 몰려 있다는 뜻이다.
대가는 합성 t-delta +32%로, (b)가 상쇄할 수 있는 종류다. TUM 단독 AbsRel은 0.1447→0.1358.
다만 §4.17의 32px 상수 oracle(0.1459)과는 프로토콜이 달라 직접 비교로 쓰지 않는다.

후속(`work_dirs/tier1b.sh`): warp 1.0 단독, edge 2.0 + warp 1.0, edge 2.0 + warp 2.0 3 arm 뒤
**실촬 AbsRel이 대조군 +1σ 이내인 arm 중 실촬 TCE 최소**를 자동 선택해 60k(`work_dirs/v9-60k`)로
잇는다.

### 4.22 T2-9 — 버킷 패딩으로 희소 경로가 compiled dense를 다시 앞선다 (2026-08-06)

> **후속(§4.24)에서 뒤집힘.** 아래 수치는 청크 스캔 시절의 기록이다. 융합 커널 이후에는
> 모든 active에서 compiled dense가 더 빠르며, 현재 속도 수치는 §4.24c·§4.27b를 인용할 것.

`pad_to_bucket`이 모아온 active 토큰 수를 64의 배수로 올리고 패드를 Δ-gating으로 끈다. 결과는
불변이고(테스트로 고정) 얻는 것은 **shape이 4종으로 고정된다**는 것뿐이다. 그 위에서
`compile_sparse()`가 스캔만 컴파일한다 — gather는 `nonzero`라 출력 shape이 데이터 의존적이라
영원히 eager다. 4090 단독 점유, 256px, 1스트림, fp32, min-of-3:

| active% | dense eager | dense compiled | sparse eager | sparse bucket eager | **sparse bucket compiled** |
|---:|---:|---:|---:|---:|---:|
| 5 | 11.44 | 4.70 | 3.07 | 5.06 | **2.93** |
| 10 | 11.37 | 4.70 | 3.80 | 5.01 | **2.88** |
| 22 | 11.38 | 4.70 | 4.87 | 5.39 | **2.99** |
| 32 | 11.42 | 4.70 | 5.92 | 7.33 | **3.94** |
| 50 | 11.37 | 4.70 | 7.48 | 8.36 | 4.58 |
| 70 | 11.43 | 4.69 | 9.43 | 9.68 | 5.38 |

(ms/frame. 버킷만 켜고 컴파일하지 않으면 **항상 느리다** — 패딩은 순수 비용이고 값은 컴파일에서
나온다.) §4.19f가 남긴 "실촬 평균에서 희소 경로가 compiled dense와 동률"은 해소됐다:
active 22%에서 2.99 ms(334 FPS)로 **1.57배**, `dense_above` 정책이 적용된 실촬 평균 32%에서도
3.94 ms(254 FPS)로 1.19배다. 다만 PLAN의 합격선(compiled dense의 절반, 2.34 ms)에는 미달이고,
**active 50%에서 이미 동률·70%에서는 dense가 낫다** — §4.20a의 dense 폴백이 이 곡선의 오른쪽
끝을 자동으로 처리한다는 점에서 두 결정이 맞물린다. fp16은 속도 이득이 없고(launch-bound)
VRAM 75→42 MB, 스트림당 state 12.75→6.38 MB만 준다.

### 4.23 T1-8 — v9-60k 확정, edge 단독 arm 폐기 (2026-08-06~07)

§4.21의 후속 큐(`work_dirs/tier1b.sh`)가 8k 3 arm을 돌린 뒤 선택자("실촬 AbsRel이 대조군
+1σ 이내인 arm 중 실촬 TCE 최소")로 **edge 2.0 + warp 2.0**을 골라 60k로 이었다
(`work_dirs/v9-60k`, 08-06 11:57 → 08-07 01:08, **13h11m**, seed 0, `main_v8` partial init).
비교 대상은 직전 확정 체크포인트인 §4.19g의 `v8-teacherfree-60k`, 프로토콜은 동일
(소스당 100클립, 8프레임, dense 폴백 on).

**(a) 실촬 — 연산 증가 없이 전 지표 개선.** active%가 소스별로 동일(19.7/44.7)해서 연산
비용은 완전히 같다.

| | active% | AbsRel ↓ | RMSE ↓ | δ1 ↑ | t-delta ↓ | OPW ↓ | TCE ↓ |
|---|---:|---:|---:|---:|---:|---:|---:|
| v8-teacherfree-60k, tum | 19.7 | 0.1338 | 0.7927 | 0.8379 | 0.1161 | 0.0227 | 0.0336 |
| **v9-60k, tum** | 19.7 | **0.1321** | **0.7685** | **0.8426** | **0.0935** | **0.0225** | **0.0333** |
| v8-teacherfree-60k, bonn | 44.7 | 0.1927 | 0.4631 | 0.8043 | 0.0669 | 0.0314 | 0.0365 |
| **v9-60k, bonn** | 44.7 | **0.1869** | **0.4440** | **0.8098** | **0.0567** | **0.0262** | **0.0314** |
| v8-teacherfree-60k, MEAN(src) | 32.2 | 0.1633 | 0.6279 | 0.8211 | 0.0915 | 0.0271 | 0.0351 |
| **v9-60k, MEAN(src)** | 32.2 | **0.1595** | **0.6063** | **0.8262** | **0.0751** | **0.0243** | **0.0323** |

MEAN(src) 기준 AbsRel −2.3%, δ1 +0.51pt, t-delta −18%, OPW −10%, TCE −8%. AbsRel·δ1 개선은
seed 노이즈(±0.005 / ±0.004) 경계에 걸쳐 단독으로는 약하지만, **두 소스에서 6개 지표가 모두
같은 방향으로 움직였고 시간 지표 3종은 노이즈를 크게 넘는다**.

**(b) 합성 — RMSE와 시간 지표가 같이 내려간다.**

| | active% | AbsRel ↓ | RMSE ↓ | δ1 ↑ | t-delta ↓ | OPW ↓ | TCE ↓ | drift ↓ |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| v8-teacherfree-60k | 49.6 | 0.3826 | 15.18 | 0.4651 | 0.2239 | 0.0452 | 0.0759 | 0.0934 |
| **v9-60k** | 49.6 | **0.3791** | **14.22** | 0.4821 | 0.2242 | **0.0308** | **0.0622** | **0.0793** |

RMSE −6.3%(§4.21c의 edge 효과가 60k에서도 유지), OPW −32%, TCE −18%. 합성 δ1 +1.7pt는
§4.20b의 seed 표준편차 ±0.015 안이라 **주장 근거로 쓰지 않는다**. t-delta는 동결 — 8k에서
edge가 만들던 t-delta +32% 열화를 warp가 정확히 상쇄했다는 §4.21b의 예측이 맞았다.

**(c) 8k 프로브가 60k를 예측하지 못했다.** 8k에서 edge2+warp2는 실촬 AbsRel 0.1822로 대조군
(0.1773)보다 **나빴고**, warp를 올릴수록 실촬 정확도가 단조 악화하는 것이 §4.21b의 관찰이었다.
60k에서는 그 교환이 사라진다(0.1595 < 0.1633). warp residual은 수렴 후기에 비용을 회수하는
항으로 보이며, **8k arm 스크리닝은 손실 항의 부호를 정하는 데는 쓸 수 있어도 크기를 정하는
데는 못 쓴다**는 것이 이번 라운드의 방법론적 교훈이다.

**(d) 귀인 한계 — 이 표는 손실 ablation이 아니다.** `v9-60k`는 `main_v8`(teacher 60k)에서,
대조군은 `main_v7`에서 partial init됐다. 둘은 init도 누적 step 수도 다르므로 (a)(b)의 차이는
"신설 손실 2종의 효과"가 아니라 **"신설 손실 2종 + 다른 init + 추가 60k"의 합**이다. 같은
init·같은 step의 깨끗한 손실 ablation은 §4.21의 8k 표뿐이고, 거기서 결론은 (c)대로 반대였다.
따라서 v9-60k는 **현재 최고 체크포인트**로만 확정하고, "warp/edge 손실이 60k에서 정확도를
개선한다"는 인과 주장은 하지 않는다.

**(e) edge 단독 60k(`v9-edge-60k`) 폐기.** `tier1c.sh`는 "8k에서는 edge 단독이 정확도 최고
(0.1745)"를 근거로 정확도 우선 대조 arm을 큐에 넣었으나, 08-07 08:10 step **23100/60000**에서
프로세스가 죽은 채 방치됐다(원인 미상, 로그 정상 종료 흔적 없음). (c)에서 그 근거 자체가
무너졌으므로 — 8k 순위가 60k에서 뒤집혔고 edge2+warp2가 실촬 정확도·시간 지표를 모두 가져갔다
— 재개하지 않고 폐기한다. 남은 큐 항목이던 32프레임 drift eval도 함께 미실행으로 남긴다.
체크포인트 디렉터리는 지우지 않고 부분 학습 상태로 보존한다.

**(f) 외부 모델 대비 위치 갱신.** 실촬 MEAN(src), §4.20c와 같은 프로토콜:

| | 파라미터 | AbsRel ↓ | δ1 ↑ | t-delta ↓ | TCE ↓ |
|---|---:|---:|---:|---:|---:|
| DA3-Base | 0.12B | **0.1244** | **0.8790** | 0.1024 | **0.0252** |
| DA v2 Small | 24.8M | 0.2256 | 0.9050 | 0.9920 | 0.1015 |
| **v9-60k (우리)** | 4M | 0.1595 | 0.8262 | **0.0751** | 0.0323 |

DA3 대비 정확도 열위는 유지되나 격차가 좁혀졌고(AbsRel 상대 +31% → **+28%**), 원시 t-delta
우위는 1.12배 → **1.36배**로 벌어졌다. 동급 크기 DA v2 Small 대비 구도(δ1만 열위)는 그대로다.
**`work_dirs/v9-60k/latest.pt`를 현행 확정 체크포인트로 삼는다.** §4.22의 wall-clock은
`v8-teacherfree-60k`로 측정했으나 구조가 동일(손실만 다름)하므로 그대로 유효하다.

### 4.24 T2-10 — 융합 Triton 스캔이 병목을 없앴고, 그 결과 희소 경로의 우위도 없앴다 (2026-08-18)

**(a) 병목은 gather가 아니라 스캔이었다.** §4.22는 "gather(`nonzero`)가 eager로 남는 한
이 이상은 안 나온다"로 닫았는데, 실제로 재보니 틀렸다. active 22% 희소 프레임(5.25 ms,
eager)의 내역은 spatial 블록 2개가 4.17 ms이고 그중 스캔(`_mix`)이 **3.75 ms — 프레임의
71%**다. gather·scatter는 전부 합쳐 0.3 ms에 불과했다.

원인은 ssm.py의 청크 스캔이 청크마다 (B, C, C, P, S) 쌍별 감쇠 텐서를 실체화한다는 것이다.
L=64·P=384·S=16에서 청크당 ~25 MB를 움직여 400 kMAC을 계산한다 — 연산이 아니라 메모리
트래픽이다.

**(b) `sokkanaem/scan_triton.py` — 재귀를 레지스터에 유지하는 융합 커널.** 쌍별 텐서를
만들지 않고 $h_l = h_{l-1}\exp(\Delta_l A) + (\Delta_l x_l)B_l$, $y_l = \sum_s h_l C_l$ 을
그대로 돈다. 학습은 미분 가능한 참조 경로를 계속 쓰고(커널은 forward 전용), 추론에서만
`torch.is_grad_enabled()`가 꺼져 있을 때 대체된다.

Δ-게이팅의 정확성은 **커널에서도 그대로**다: $\Delta=0$이면 $\exp(0)=1$, 입력항은 정확히 0이라
상태가 비트 단위로 복사된다(`tests/test_scan_kernel.py`). 참조 스캔과의 수치 차이는
상대 1e-7 수준(fp32 반올림)이고, **실촬 평가 지표는 소수점 4자리까지 완전히 동일**하다
(AbsRel 0.1595 / δ1 0.8262 / TCE 0.0323 — §4.23a와 같은 값).

**(c) 실측 — 모든 경로가 빨라졌고, 순위가 뒤집혔다.** 4090 단독 점유, 256px, 1스트림, fp32,
min-of-3. 괄호는 §4.22(청크 스캔) 대비 배수.

| active% | dense eager | **dense compiled** | sparse eager | sparse bucket eager | sparse bucket compiled |
|---:|---:|---:|---:|---:|---:|
| 5 | 1.97 (5.8×) | **1.287** (3.7×) | 2.35 | 2.55 | 2.03 (1.4×) |
| 22 | 1.98 (5.7×) | **1.290** (3.6×) | 2.40 | 2.55 | 2.04 (1.5×) |
| 32 | 1.98 (5.8×) | **1.288** (3.6×) | 2.38 | 2.63 | 2.09 (1.9×) |
| 50 | 1.98 (5.7×) | **1.289** (3.6×) | 2.39 | 2.62 | 2.12 (2.2×) |
| 70 | 1.98 (5.8×) | **1.288** (3.6×) | 2.41 | 2.57 | 2.20 (2.4×) |

(ms/frame.) fp16 희소+버킷+컴파일은 1.69 ms(593 FPS)에 VRAM 37 MB·스트림당 state 6.38 MB로,
이번에는 fp16이 실제로 빨라진다(1.69 vs 2.04) — 더 이상 launch-bound만은 아니라는 뜻이다.
4스트림에서는 희소 5.04 ms/frame(794 FPS 총합), dense compiled 1.94 ms/frame(2060 FPS 총합).

**(d) 합격선은 넘겼지만 시스템 기여 주장은 무너졌다.** PLAN의 합격선(2.34 ms)은 희소
2.04 ms·dense 1.29 ms로 둘 다 통과했다. 그러나 **모든 active 비율에서 dense compiled가 희소
경로보다 빠르다**(1.29 vs 2.03~2.20). §4.22에서 1.57배였던 희소 우위가 0.63배로 뒤집혔다.

이유는 단순하다. 희소 경로가 아끼던 것이 바로 스캔이었는데, 그 스캔이 이제 거의 공짜다.
남은 것은 active 비율과 무관한 고정 비용 — gather(`nonzero`), `argsort`, 버킷 패딩,
캐시 `clone`, scatter, 그리고 `bool(mask.all())`·`idx.numel()`이 강제하는 GPU→CPU 동기화 —
이고 이 묶음이 절약한 스캔보다 비싸다. 표의 dense 행이 active 비율에 완전히 평평하고
(1.973~1.994) 희소 행도 거의 평평한 것이 그 증거다: **이 모델은 이제 연산량이 아니라
오버헤드에 묶여 있고, 4090·dim 192·256px에서 FLOPs는 더 이상 시간을 예측하지 못한다.**

정직하게 적으면 이렇다.

- **살아남은 것**: Δ-게이팅의 정확한 상태 보존(수식·테스트·정확도 불변), MAC 절감(§4.11의
  회계는 그대로 37.0%), 그리고 융합 커널 자체(dense 스트리밍이 776 FPS).
- **무너진 것**: "희소 경로가 dense보다 빠르다"는 wall-clock 주장. 4090에서는 성립하지 않는다.
- **미결**: MAC 절감이 시간·전력으로 환산되려면 하드웨어가 연산에 묶여 있어야 한다. 그것을
  가르는 측정이 정확히 T2-11(Jetson Orin)이고, 그건 아직 못 했다. 그전까지 희소 경로의
  이득은 **연산량과 메모리(스트림당 state)로만** 주장한다.

→ `sokkanaem/scan_triton.py`, `tests/test_scan_kernel.py`, `work_dirs/t2-10-bench.{sh,log}`

### 4.25 T3-13 — 실촬 주행 zero-shot: 정확도는 넘어가고, 희소성이 안 넘어간다 (2026-08-18)

학습에 쓴 적 없는 KITTI raw 5개 드라이브(885프레임, `2011_09_26` 시리즈)로 평가했다.
GT는 KITTI depth annotated(LiDAR 투영, 유효 픽셀 30%). NYU는 호스트가 응답하지 않고
ScanNet은 서명 동의가 선행이라 이번 라운드에서 제외했다 — 따라서 이 실험은 **완전한 미지
도메인이 아니라 "합성으로만 본 도메인의 실촬판"**이다(vkitti2는 학습에 포함, KITTI raw는 미포함).
그 점에서 domain gap 중에서도 **synthetic → real** 축만 격리해 잰다.

| | active% | AbsRel ↓ | RMSE ↓ | δ1 ↑ | t-delta ↓ | OPW ↓ | TCE ↓ | scale |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 실촬 KITTI raw (zero-shot) | **92.8** | **0.2894** | 11.03 | **0.4955** | 2.0716 | 0.1045 | 0.0995 | 2.630 |
| 합성 vkitti2 holdout (in-domain) | 25.8 | 0.3619 | 33.89 | 0.3943 | 0.3115 | 0.0160 | 0.0243 | 0.760 |

**(a) 정확도는 오히려 실촬이 낫다 — 다만 난이도가 다르다.** AbsRel 0.2894 vs 0.3619,
δ1 0.4955 vs 0.3943으로 zero-shot 실촬이 in-domain 합성을 앞선다. 이걸 "일반화가 잘 된다"로
읽으면 안 된다. vkitti2 holdout(Scene06)은 수백 m 구조가 있어 RMSE가 33.9까지 가고(§4.21a의
정보 한계), KITTI GT는 LiDAR라 ~80 m에서 잘리고 유효 픽셀이 30%뿐이며 대부분 화면 하단의
가까운 영역이다. **두 수치는 난이도가 다른 문제를 푼 결과**라 서열로 쓰면 안 되고, 말할 수
있는 것은 "합성 주행으로 학습한 표현이 실촬 주행에서 붕괴하지 않는다"까지다.

**(b) 기본 설정의 희소성은 넘어가지 않는다.** 같은 장면 종류인데 active가 25.8% →
**92.8%**로 3.6배다. `scripts/gate_probe.py`로 검출기만 따로 재도 같은 그림이다(폴백 제외,
같은 체크포인트·60프레임): 픽셀 tau 0.05에서 합성 Scene06은 7.2~10.3% active인데 실촬
드라이브는 39.5~73.9%로 약 6배. 렌더링 영상에 없는 센서 노이즈·노출 변동·롤링 셔터·압축
아티팩트가 전부 "변화"로 잡히기 때문으로 보이며, **§3.1~3.2의 스킵률은 합성에서 측정된
낙관값**이라는 뜻이다. 다만 이것은 *픽셀 게이팅 기본 임계*의 한계이지 방법의 한계는 아니다 —
GMC 경로가 이 손실의 대부분을 되찾는다(§4.26).

**(c) 시간 지표와 배율.** t-delta 0.3115 → 2.0716(6.6배), TCE 0.0243 → 0.0995(4.1배)로
실촬에서 크게 악화한다. median scaling 배율도 0.760 → 2.630으로 3.5배 어긋나, metric scale을
주장하지 않는 §4.20d의 입장이 도메인 이동에서 더 강해진다.

→ `work_dirs/tier3.sh`, `work_dirs/tier3-console.log`, `work_dirs/t3-14-gate-probe.log`

### 4.26 T3-14 — GMC를 실 이동 카메라에서 검증: 같은 연산에서 확실히 낫다 (2026-08-18)

W11("GMC가 합성 clean geometry에서만 검증됨")의 정면 응답. §4.25와 같은 KITTI raw 드라이브
30클립에 대해 두 게이팅 방식의 **active-정확도 곡선 전체**를 그렸다. 두 방식은 임계 스케일이
다르므로(픽셀 MSE vs 피처 상대 L1) 같은 tau 비교는 무의미하고, **같은 active 비율에서의
정확도**만이 공정한 비교다.

| 게이팅 | tau | active% | AbsRel ↓ | δ1 ↑ | t-delta ↓ | TCE ↓ |
|---|---:|---:|---:|---:|---:|---:|
| 픽셀 | 0.02 | 100.0 | 0.3083 | 0.5142 | 3.0852 | 0.1189 |
| 픽셀 | 0.05 | 92.7 | 0.3093 | 0.5089 | 3.1016 | 0.1236 |
| 픽셀 | 0.1 | 51.1 | 0.3357 | 0.4749 | 2.5108 | 0.1074 |
| GMC + 피처 | 0.2 | 87.1 | 0.3065 | 0.5170 | 3.0533 | 0.1173 |
| GMC + 피처 | 0.4 | **43.8** | **0.3084** | **0.5342** | 1.9319 | 0.0992 |
| GMC + 피처 | 0.8 | **14.1** | **0.3178** | **0.5341** | **1.2314** | **0.0922** |

**(a) 같은 연산에서 GMC가 이긴다.** active 약 50% 구간을 맞춰 보면 픽셀 51.1%가 AbsRel
0.3357·δ1 0.4749인데 GMC 43.8%는 **0.3084·0.5342** — 연산을 더 적게 쓰고 AbsRel −8.1%(상대),
δ1 +5.9pt다. 더 극단적으로, **GMC는 active 14.1%에서도 픽셀 게이팅의 51.1% 지점보다 모든
지표가 낫다**(0.3178 vs 0.3357, δ1 0.5341 vs 0.4749). 호모그래피 보정이 카메라 유발 변화를
흡수하고 실제 장면 변화만 남긴다는 §3.5의 주장이 **실촬에서 재현된다**.

**(b) 자기 자신의 100% 기준 대비 비용.** GMC 곡선 내부에서 100% → 14.1%로 연산을 1/7로
줄이는 대가는 AbsRel 0.3083 → 0.3178(+3.1% 상대)뿐이고, δ1은 오히려 0.5142 → 0.5341로
개선되며 시간 지표는 단조 개선된다(t-delta 3.09 → 1.23, TCE 0.1189 → 0.0922). §3.2가 합성에서
보고한 형태(스킵률↑ → t-delta 단조 개선)가 실촬에서도 유지된다.

**(c) 기본값은 실촬에 맞지 않는다 — 도메인별 재조정이 필요하다.** GMC 기본 임계(tau_on 0.1)는
실촬 주행에서 active 100%로 아무것도 건너뛰지 못한다. 이득은 tau 0.4~0.8 구간에서만 나온다.
즉 "GMC를 켜면 된다"가 아니라 **"GMC를 켜고 임계를 그 도메인에서 다시 잡아야 한다"**가 정확한
서술이며, §4.25b의 실촬 92.8%는 재조정을 안 한 기본값의 수치다.

**(d) 켜고 끄기 단순 비교는 무의미하다.** 기본 임계에서 GMC on/off만 바꾼 100클립 비교는
AbsRel 0.2894 → 0.2884, δ1 0.4955 → 0.4991로 사실상 동률인데 active는 92.8 → 100.0으로
오히려 늘었다. (c) 때문이다 — 이 한 쌍만 보고 "GMC는 효과 없다"고 결론내면 (a)의 곡선을
놓친다. **GMC의 검증은 단일 지점이 아니라 곡선으로만 가능하다.**

→ `work_dirs/tier3.sh`, `work_dirs/t3-14-pixel-sweep.log`, `work_dirs/t3-14-gate-probe.log`

### 4.27 개선 방향을 정하기 위한 세 측정 (2026-08-18)

다음 라운드를 정하기 전에, 추측으로 결정될 뻔한 세 가지를 먼저 쟀다. 셋 다 계획을 바꿨다.

**(a) 실촬 도메인 구조 상한 — 패치 그리드는 병목이 아니다.** `scripts/ceiling_probe.py`가
GT를 모델과 같은 병목(토큰 그리드로 pool → bilinear 복원 → median 정합)에 통과시켜, 순수
토큰 그리드 head가 낼 수 있는 최고점을 측정한다(실촬 holdout, 60클립/소스).

| 소스 | patch 16 상한 | patch 8 상한 | **현재 모델** |
|---|---|---|---|
| TUM | AbsRel **0.0844** · δ1 0.9140 | 0.0466 · 0.9619 | 0.1321 · 0.8426 |
| Bonn | AbsRel **0.0651** · δ1 0.9442 | 0.0306 · 0.9808 | 0.1869 · 0.8098 |

**현재 patch 16에서도 상한이 AbsRel 0.065~0.084이고 우리는 0.132~0.187이다 — 상한의 2~2.9배
위에 있다.** patch 16 상한(δ1 0.914~0.944)만 뽑아도 DA3(0.1244 · 0.8790)를 넘는다. 즉
**해상도·패치 크기·출력 구조는 지금 병목이 아니고, 병목은 용량·학습·데이터다.** 더 잘게
쪼개는 데 연산을 쓰는 것은 현 시점에서 잘못된 투자다.

> 주의: 이 표는 **유효 픽셀만 pool**한 값이다. 첫 실행은 마스킹 없이 pool해 실촬 depth의
> 구멍(0)이 그대로 섞였고, disparity 공간에서는 1/eps가 패치를 지배해 TUM AbsRel 11.4라는
> 무의미한 수를 냈다. 그 상태의 depth 공간 값(TUM 0.1388)은 "우리가 상한을 이미 넘었다"는
> 정반대 결론을 만들 뻔했다.

**(b) 실측 처리량 — DA v2 Small 대비 이미 앞선다, 다만 §3.4는 양쪽 다 낡았다.** 같은 GPU,
256px, batch 1:

| | fp32 eager | fp32 compiled | **fp16 compiled** | VRAM(fp16) |
|---|---:|---:|---:|---:|
| DA v2 Small (24.8M) | 3.41 ms / 293 FPS | 2.23 ms / 449 FPS | 0.816 ms / **1226 FPS** | 49.6 MB |
| **본 모델 dense (4.19M)** | 1.98 ms / 505 FPS | 1.29 ms / 776 FPS | **0.378 ms / 2646 FPS** | **20.6 MB** |

**fp16 compiled에서 2.2배 빠르고 VRAM은 2.4배 적다.** 그리고 이 값은 active 70%에서도
0.371 ms(2693 FPS)로 동일하다 — dense 경로라 활성 비율과 무관하기 때문이다. 정확도도
0.1595 vs 0.2256으로 우리가 앞선다. **"active 50% 초과 구간에서 DA v2 Small 이상의 효율"은
이미 달성돼 있다.** 단 그것은 희소 경로를 포기하고 dense로 돌 때의 이야기다(§4.24d).

§3.4의 "DA V2 Small 380 FPS vs 본 모델 366 FPS(동급)"는 **양쪽 다 낡았다** — 우리는 융합
커널로 2646까지 갔고, DA v2도 최신 컴파일 경로에서 1226이다. 그 표는 v3 시절 수치이므로
인용하지 않는다.

**(c) 남은 정확도 격차의 소재.** 현재 실촬 MEAN 0.1595 중 Bonn이 0.1869로 TUM(0.1321)보다
훨씬 나쁘고, Bonn의 상한 여유가 가장 크다(0.1869 → 0.0651). **Bonn을 상한 쪽으로 절반만
당겨도 MEAN이 0.1595 → 0.13 아래로 내려가 DA3(0.1244)에 근접한다.** Bonn은 동적 객체·가림이
많은 소스이므로, 다음 정확도 작업의 1순위는 해상도가 아니라 **동적 장면 처리**다.

→ `scripts/ceiling_probe.py`, `work_dirs/t2-10-bench.log`

### 4.28 논문용 표 재생성 — 그리고 토큰 제거 대조군이 더 이상 실패하지 않는다 (2026-08-18)

초안이 v3·v7·v9 세 체크포인트의 표를 섞어 쓰고 있었다(§5.1은 v7, §5.3·§5.4는 v3, 나머지는 v9).
심사자의 첫 질문이 "이 논문은 어느 모델 이야기인가"가 되므로, 활성 스윕과 게이팅 위치 ablation을
확정 체크포인트에서 다시 돌렸다.

**(a) 활성–정확도 교환, 확정 체크포인트.** 소스당 100클립, MEAN(src).

| | 합성 active% | 합성 AbsRel | 합성 δ1 | 실촬 active% | 실촬 AbsRel | 실촬 δ1 | 실촬 t-delta |
|---|---:|---:|---:|---:|---:|---:|---:|
| τ=0.005 | 70.1 | 0.3734 | 0.4941 | 96.2 | 0.1552 | 0.8373 | 0.0836 |
| τ=0.02 | 60.2 | 0.3768 | 0.4880 | 74.0 | **0.1545** | 0.8365 | 0.0994 |
| τ=0.05 (기본) | 49.6 | 0.3791 | 0.4821 | 32.2 | 0.1595 | 0.8262 | 0.0751 |
| τ=0.1 | 39.2 | 0.3814 | 0.4807 | **7.1** | 0.1753 | 0.8106 | **0.0299** |
| 상수 대조군 | — | 0.5504 | 0.3574 | — | 0.3581 | 0.5957 | 0.0000 |

**실촬에서 활성 96.2% → 7.1%로 연산을 13분의 1로 줄이는 대가가 AbsRel 0.1552 → 0.1753
(상대 +13%)뿐이고, 기본 설정인 32.2%에서는 +2.8%다.** τ=0.02(활성 74%)에서는 오히려 전체
연산보다 정확도가 좋다(0.1545 vs 0.1552) — 게이팅이 stale context를 끊는 효과로 보인다.
상수 대조군(0.3581)과의 거리는 전 구간에서 크게 유지된다.

합성 스윕이 39.2% 아래로 못 내려가는 것은 TartanAir가 τ와 무관하게 80~100% 활성이기 때문이다
(§4.25b의 "촬영 조건이 스킵을 결정한다"와 같은 현상).

**(b) 토큰 제거 대조군이 더 이상 실패하지 않는다 — 기존 주장 하나를 철회한다.** 같은 마스크
(활성 49.6%), 같은 체크포인트, temporal cache는 양쪽 다 끔:

| gate_mode | AbsRel ↓ | RMSE ↓ | δ1 ↑ | t-delta ↓ | OPW ↓ | TCE ↓ |
|---|---:|---:|---:|---:|---:|---:|
| Δ-gating | 0.3791 | 14.22 | 0.4821 | **0.2251** | **0.0309** | **0.0622** |
| 토큰 제거 | **0.3771** | **13.98** | **0.4866** | 0.2684 | 0.0337 | 0.0649 |

v3에서는 토큰 제거가 AbsRel 1.7178 vs 0.4292로 **4배** 무너졌다(§4.14). 확정 체크포인트에서는
**정확도가 사실상 동률이거나 오히려 토큰 제거가 근소 우위**이고, 차이는 전부 시간 지표에만
남는다 — t-delta +19%, OPW +9%, TCE +4%.

원인은 학습 경로의 변화로 본다. v3는 희소 경로가 학습에 포함되지 않은 추론 전용 근사였고,
v6부터 캐시 경로가 학습에 들어가면서 모델이 정적 토큰의 결손에 강건해졌다. 즉 **v3의 4배
붕괴는 "보존된 상태 readout의 가치"가 아니라 "학습되지 않은 희소 경로의 취약성"을 재고 있었다.**

논문 주장을 이에 맞춰 좁힌다: **보존된 상태를 읽는 것은 정확도가 아니라 시간 안정성을 위해
필요하다.** 기여 목록의 "readout이 없으면 정확도 4배 악화"는 확정 체크포인트에서 재현되지
않으므로 철회하고, v3 수치는 학습되지 않은 희소 경로의 기록으로만 인용한다.

→ `work_dirs/paper-tables.{sh,log}`, `work_dirs/paper-drop.{sh,log}`

### 4.29 대중적 비교군 전체를 한 프로토콜로 재측정 — 그리고 DA v2 Small 수치가 아티팩트였다 (2026-08-19)

기존 비교표는 DA3와 DA v2 Small 둘뿐이었다. 널리 인용되는 비교군(DPT/MiDaS 계열, Depth
Anything 세 크기, metric 모델)을 **전부 우리 프로토콜로** 돌렸다 — 같은 holdout 클립, 256px,
같은 클립별 정합, 같은 지표 구현(`scripts/eval_baseline_da2.py`가 `--ckpt/--mode`로 일반화됨).

**(a) 결정적 주의 — 정렬 자유도가 다르다.** 상대 깊이 모델은 disparity 공간 최소자승
scale+shift(2 DOF, MiDaS 관행)로, metric 모델과 우리는 클립별 median(1 DOF)으로 정합한다.
2 DOF가 항상 유리하므로 **정렬을 섞어 AbsRel을 직접 비교하면 프로토콜이 결과를 만든다.**
이를 분리하려고 `eval.py --align scaleshift`를 추가해 **우리 모델도 baseline과 같은 규칙으로**
측정했다.

| 모델 | 정렬 | 실촬 AbsRel | 실촬 medAbsRel | 실촬 δ1 | 실촬 t-delta | 실촬 OPW | 실촬 TCE |
|---|---|---:|---:|---:|---:|---:|---:|
| DA-v1-Small (24.8M) | 2 DOF | **0.0736** | **0.0637** | **0.9350** | 0.1074 | 0.0247 | 0.0303 |
| ZoeDepth-NK (345M) | 1 DOF | 0.0997 | 0.0930 | 0.8910 | 0.1013 | 0.0241 | 0.0291 |
| DPT-Large (343M) | 2 DOF | 0.1002 | 0.0973 | 0.9112 | 0.1371 | 0.0335 | 0.0389 |
| DA-v2-Base (97.5M) | 2 DOF | 0.1060 | 0.0895 | 0.9182 | 0.4052 | 0.0364 | 0.0425 |
| DA3-Base (0.12B) | 1 DOF | 0.1244 | 0.1154 | 0.8789 | 0.1024 | **0.0185** | **0.0252** |
| DA-v2-Small (24.8M) | 2 DOF | 0.2256 | 0.0967 | 0.9050 | 0.9919 | 0.0958 | 0.1014 |
| **본 모델 (4.19M)** | 1 DOF | 0.1595 | 0.1041 | 0.8262 | **0.0751** | 0.0244 | 0.0324 |
| **본 모델 (4.19M)** | 2 DOF | 0.1321 | — | 0.8268 | **0.0715** | 0.0229 | 0.0309 |

| 모델 | 합성 AbsRel | 합성 δ1 | 합성 t-delta | 합성 OPW | 합성 TCE |
|---|---:|---:|---:|---:|---:|
| ZoeDepth-NK | **0.3604** | 0.5021 | 0.9522 | 0.1068 | 0.1298 |
| DA3-Base | 0.3618 | 0.4409 | 1.0128 | 0.0482 | **0.0593** |
| DA-v2-Base | 0.3701 | **0.7441** | 5.9644 | 0.1267 | 0.1409 |
| **본 모델** | 0.3791 | 0.4821 | **0.2242** | **0.0308** | 0.0622 |
| DA-v2-Small | 0.3818 | 0.7301 | 5.0124 | 0.1104 | 0.1265 |
| DA-v1-Small | 0.4210 | 0.7109 | 4.5525 | 0.1200 | 0.1383 |
| DPT-Large | 0.5029 | 0.6766 | 8.5888 | 0.1458 | 0.1669 |

**(b) DA v2 Small의 실촬 0.2256은 정렬 아티팩트다 — 기존 주장을 철회한다.** 클립별 분포를
보면 TUM에서 **평균 0.3463, 중앙값 0.0950, 최댓값 6.021**이다. 소수 클립에서 정합된 disparity가
0 근처로 가고 역수를 취하면서 깊이가 폭주해 평균을 지배한다. 중앙값 0.0950은 다른 baseline과
정상 범위이며, 같은 24.8M의 DA-v1-Small(0.0637)과도 어긋나지 않는다.

즉 §4.20c와 논문 초안의 **"동급 파라미터(24.8M) 대비 δ1만 빼고 전부 우위"는 성립하지 않는다.**
그 주장은 폭주한 소수 클립이 만든 수치에 기대고 있었다. 강건 통계로 보면 DA v2 Small은 실촬
정확도에서 우리보다 낫다(medAbsRel 0.0967 vs 0.1041).

**(c) 정직하게 남는 것.** 실촬에서 **우리는 정확도 최하위권**이다 — AbsRel은 2 DOF로 맞춰도
0.1321로 DA-v1-Small(0.0736)의 1.8배, δ1은 0.8268로 전 비교군 중 최하다. 우리가 1위인 것은
**원시 프레임 간 변동(t-delta) 하나**이고(0.0751, 차순위 ZoeDepth 0.1013 대비 1.35배),
OPW·TCE는 DA3에 뒤진다.

합성에서는 그림이 다르다 — **t-delta 0.2242로 차순위(0.9522)의 4.2배, OPW 0.0308로 1위**이고
AbsRel은 0.3791로 4위지만 최상위(0.3604)와 5% 이내다. 파라미터가 6~82배 적다는 점을 같이
적어야 의미가 있다.

**(d) 결론적으로 무엇을 주장할 수 있나.** "동급 크기 대비 정확도 우위"는 **철회**한다.
방어 가능한 주장은 셋이다 — (1) 4.19M으로 6~82배 큰 모델들의 정확도에 같은 자릿수로 근접,
(2) **후처리 없는 원시 플리커 억제는 전 비교군 중 1위**, (3) 반정밀도 2646 FPS로 처리량 1위
(§4.27b). 정확도 우위는 이 목록에 없다.

→ `work_dirs/baseline-suite.{sh,log}`, `work_dirs/zoe-rerun.log`, `work_dirs/ours-scaleshift.log`

### 4.30 정확도 병목의 실제 소재 — 시간 상태가 정확도를 **깎고** 있다 (2026-08-19)

"정확도를 어떻게 올릴 것인가"를 정하기 전에, 아무도 재지 않았던 것을 쟀다: **스트리밍 상태가
정확도에 기여하는가?** 프레임을 가로질러 증거를 누적하는 모델이라면 프레임 7이 프레임 0보다
정확해야 한다. 그것이 상태를 유지하는 이유다.

클립 내 프레임 인덱스별 AbsRel(프레임마다 독립적으로 median 정합해 클립 단위 정합의
아티팩트를 배제):

| 프레임 | 0 | 4 | 8 | 12 | 16 | 20 | 24 | 28 | 31 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| TUM | 0.1353 | 0.1206 | 0.1249 | 0.1510 | 0.1521 | 0.1415 | 0.1585 | 0.1697 | **0.1323** |
| Bonn | 0.1642 | 0.1690 | 0.1795 | 0.1934 | 0.2185 | 0.2312 | 0.2431 | **0.2636** | **0.2011** |

**(a) 누적 이득은 없고 누적 손실이 있다.** TUM은 초반에 5%가량 좋아졌다가 이후 악화하고,
**Bonn은 프레임 0의 0.1642에서 프레임 28의 0.2636까지 단조 악화한다 — +61%**다. 즉 이 모델의
시간 상태는 깊이 정확도를 **개선하지 않으며**, 동적 장면에서는 적극적으로 **악화시킨다.**

**(b) 프레임 31의 회복은 키프레임이다.** `keyframe_every=30`이라 프레임 30에서 전체 재계산이
일어나고 정확도가 즉시 복구된다(Bonn 0.2636 → 0.2011, TUM 0.1697 → 0.1323). **톱니 패턴이다** —
게이팅이 키프레임 사이에서 오차를 누적하고, 키프레임이 그것을 지운다. 즉 정확도 손실의 상당
부분은 용량이 아니라 **키프레임 사이의 드리프트**이며, 이미 해법을 갖고 있으면서 너무 드물게
쓰고 있다.

**(c) 보고된 수치는 스트리밍에 대해 낙관적이다.** 우리의 모든 정확도 수치는 **8프레임** 클립
평균이다. 8프레임 구간은 톱니의 가장 좋은 부분만 본다. 30프레임 창에서는 Bonn 평균이 눈에
띄게 나빠진다. §4.20d의 8→32프레임 배율 드리프트 증가(실촬 0.0235→0.0444)와 같은 현상의
정확도 측면이다. **배포가 수백 프레임이라면 8프레임 수치를 인용하면 안 된다.**

**(d) 학습–배포 불일치가 원인일 가능성이 크다.** 학습은 8프레임 클립으로 한다. 모델은 30프레임
동안 게이팅을 견디는 법을 **한 번도 배운 적이 없다.** 무작위 마스크 스케줄링은 희소성의 *정도*에
강건하게 만들지만 희소성의 *지속*에는 그렇지 않다.

이 절이 PLAN Tier 5(정확도) 우선순위를 결정한다 — 첫 번째 지렛대는 모델을 키우는 것이 아니라
**스트리밍 상태가 시간에 따라 무너지는 것을 막는 것**이다.

→ 측정 스크립트는 `scripts/frame_index_probe.py`

### 4.31 T5-25 키프레임 주기 — 드리프트의 상당 부분은 스케줄링만으로 회수된다 (2026-08-19)

§4.30의 톱니는 해법이 이미 모델 안에 있고 너무 드물게 쓰인다는 뜻이다. 실촬 holdout,
**32프레임 클립**(8프레임으로는 30주기를 볼 수 없다), 30클립/소스:

| keyframe_every | active% | AbsRel ↓ | δ1 ↑ | t-delta ↓ | TCE ↓ |
|---:|---:|---:|---:|---:|---:|
| 5 | 45.0 | **0.1551** | **0.8280** | 0.1084 | 0.0368 |
| **10** | **35.3** | **0.1601** | **0.8233** | 0.0914 | 0.0355 |
| 15 | 32.4 | 0.1648 | 0.8142 | 0.0879 | 0.0357 |
| 30 (현재 기본) | 29.2 | 0.1774 | 0.7985 | 0.0813 | 0.0356 |
| 60 | 26.2 | 0.1807 | 0.7937 | **0.0697** | **0.0330** |

**(a) 32프레임에서 현재 기본값은 0.1774다.** 우리가 보고해 온 8프레임 수치는 0.1595 —
§4.30c의 "8프레임 수치는 스트리밍에 낙관적"이 정량적으로 확인된다. 같은 체크포인트·같은
소스인데 클립 길이만 8→32로 바꾸면 AbsRel이 **11% 나빠진다.**

**(b) 30 → 10이 좋은 지점이다.** AbsRel 0.1774 → 0.1601로 **상대 −9.8%**, δ1 +2.5pt를
active +6.1pt로 산다. §4.27b의 연산 예산(2.2배)에 비하면 싼 값이다.

**(c) 5까지 가면 안 되는 이유.** AbsRel은 0.1551로 더 좋아지지만 **t-delta가 0.1084로 올라
우리의 유일한 1위 지표를 잃는다** — §4.29의 비교군에서 ZoeDepth 0.1013, DA3 0.1024보다 나빠진다.
키프레임은 정의상 불연속이므로 자주 칠수록 플리커가 는다. **10에서는 0.0914로 여전히 1위다.**
정확도와 유일한 우위 지표 사이의 교환이며, 10이 둘 다 지키는 지점이다.

**(d) 기본값 변경은 T5-24 이후로 미룬다.** 지금 30 → 10으로 바꾸면 기존 보고 수치와의
비교 가능성이 깨지고, 무엇보다 긴 클립 학습(T5-24)이 최적 주기를 바꿀 가능성이 높다 —
드리프트를 견디도록 학습된 모델은 갱신이 덜 필요해야 한다. 재학습 후 이 스윕을 다시 돌려
결정한다.

→ `work_dirs/t5-keyframe.{sh,log}`, `scripts/eval.py --keyframe-every`

### 4.32 T5-32 계통 오차의 정체 — 예측 깊이의 동적 범위가 압축돼 있다 (2026-08-19)

§4.29a에서 2 DOF 정렬이 실촬 AbsRel을 0.1595 → 0.1321로 17% 개선한 것은, 1 DOF가 못 잡는
계통 오차가 있다는 뜻이다. 클립 단위로 쪼개보니 **소스 특이적**이었다:

| 소스 | 2 DOF가 이득인 클립 | 중앙 개선 |
|---|---:|---:|
| TUM | 28/89 (31%) | −5.7% (오히려 손해) |
| **Bonn** | **78/100 (78%)** | **+9.6%** |

**(a) 원인은 동적 범위 압축이다.** 같은 클립에서 예측과 GT의 disparity 분포 폭(5–95%)을 비교:

| 소스 | 예측 폭 | GT 폭 | 비율 | std 비율 |
|---|---:|---:|---:|---:|
| TUM | 0.4068 | 0.4433 | 0.92 | 0.87 |
| **Bonn** | 0.4717 | **1.0068** | **0.47** | **0.44** |

**Bonn에서 모델이 내는 깊이 필드의 동적 범위가 GT의 절반도 안 된다.** GT 폭 자체가 TUM의
2.3배(1.0068 vs 0.4433)인데 모델은 그 범위를 만들어내지 못하고 평균 쪽으로 회귀한다.
δ1이 낮은 것(비율 기반 지표)과 Bonn이 상한의 2.9배 위인 것이 같은 하나의 현상이다.

**(b) 디코딩 아티팩트가 아니다 — 값싼 수정은 실패했다.** bin head가 softmax 기댓값이라
(`p = head(x).softmax(1)`, 로그 깊이 bin 중심에 대한 기댓값) 불확실하면 분포 평균으로 끌린다.
이건 재학습 없이 **추론 시 온도만 낮춰** 검증할 수 있어서 T ∈ {1.0, 0.7, 0.5, 0.35, 0.25}를 쓸어봤다:

| T | Bonn AbsRel | Bonn δ1 | Bonn 범위비 |
|---:|---:|---:|---:|
| 1.00 | 0.2618 | 0.7315 | 0.58 |
| 0.50 | 0.2642 | 0.7320 | 0.59 |
| 0.25 | 0.2677 | 0.7278 | 0.58 |

**범위비가 사실상 안 움직이고 AbsRel은 오히려 나빠진다.** 즉 bin 분포는 이미 충분히 뾰족하고,
압축은 기댓값 연산이 아니라 **예측된 bin 중심 자체가 평균 쪽에 몰려 있다**는 뜻이다. 모델이
실제로 압축된 필드를 예측한다.

**(c) 따라서 값싼 파라미터화 수정은 없다.** 남는 원인 후보는 (i) 불확실할 때 평균으로 회귀하는
회귀 손실의 성질 — `si_log_loss`는 분산 항에 0.5 계수를 두어 평균 오차 벌점을 완화한다,
(ii) 용량 부족으로 극단값에 확신을 못 갖는 것, (iii) 학습 데이터의 깊이 범위 다양성 부족.
셋 다 Tier 5 B군(용량·데이터)로 연결되며, **긴 클립 학습(T5-24)도 이 문제는 고치지 못한다** —
드리프트와 범위 압축은 별개의 결함이다.

새 후보 항목: 예측 분포의 폭을 GT에 맞추는 명시적 항(분산/히스토그램 정합)을 추가하는 것.
현재 손실에는 범위 압축을 벌하는 항이 없다.

→ 온도 스윕과 폭 측정은 일회성 진단, 수치는 이 절에 보존

## 5. 한계 및 미검증 사항

- PoC 해상도(128px, 정사각 resize) + 30k step 제약으로 절대 정확도($\delta_1$ 0.68)는 낮음 — trade-off
  곡선의 **형태**가 검증 대상이었고, 절대 성능은 본 학습(§4)에서 확보 예정.
- §3.5 GMC+feature gating은 **실촬 이동 카메라(KITTI raw)에서 검증됐다**(§4.26): 같은 active에서
  픽셀 게이팅을 이기고 active 14.1%까지 내려간다. 다만 기본 임계는 실촬에서 무의미해
  도메인별 재조정이 전제이고, 검증 범위는 주행 영상 5드라이브 30클립뿐이다.
- Jetson Orin 실측 미실시 (로드맵 3단계, 미착수). 4090에서는 모델이 launch-bound라
  (§4.11) FLOPs 절감이 FPS로 안 나타남 — 에지 전력 이득 주장은 Jetson 실측 없이는 미검증.
- TAE(pose 워프 기반 시간 지표) 미구현 — 어댑터에 extrinsic/intrinsic 배관 필요(§4.10).
- 실촬 학습·평가는 TUM/Bonn holdout 시퀀스로 확보됐다(§4.18–4.19) — 같은 데이터셋의 다른
  시퀀스라 sequence holdout이다. **외부 도메인 zero-shot은 KITTI raw로 별도 확보**했고
  (§4.25), 거기서 정확도는 붕괴하지 않지만 **기본 설정의 희소성이 넘어가지 않는다**
  (active 25.8% → 92.8%). 실내 미지 도메인(NYU·ScanNet)은 호스트 무응답·서명 요건으로 미실시.
- 수백 프레임 초장기 스트림 드리프트는 270프레임 범위까지만 확인.
- **§3.1~3.2의 스킵률은 합성 영상에서 측정된 낙관값이다**(§4.25b). 같은 검출기·같은
  체크포인트로 재면 픽셀 tau 0.05에서 합성 7~10% active가 실촬에서 40~74%로 약 6배가 된다 —
  센서 노이즈·노출 변동·롤링 셔터가 전부 변화로 잡힌다. 고정 카메라 실내(19.7~44.7%)는
  실측이지만, 실촬 주행에서 기본 임계로는 스킵이 사실상 없다.
- 패치 크기(8/16/32), 게이팅 위치(Δ-gating vs 토큰 drop vs 출력 캐시만) ablation은 모델 개조 필요로 보류.
- 외부 baseline 공통 비교는 DA3-Base·DA v2까지 60k 체크포인트 기준으로 재생성됐다(§4.20c).
  **VDA는 로컬 체크아웃 소실로 여전히 §4.15(v3 시절) 수치**이고, 그 외 2025~2026 스트리밍
  모델(예: video-depth 계열 신규 발표)은 재현 자체를 시도하지 않았다.
- seed 반복은 8k step 프로브 6회로만 확보(§4.20b). **60k 본 학습은 여전히 조건당 1회**이므로
  60k 수준의 절대 수치에는 신뢰구간이 없다. 합성 δ1은 8k에서 seed 표준편차 ±0.015였다.
- 확정 체크포인트 `v9-60k`와 그 대조군은 init 계보와 누적 step이 달라 **60k 수준의 손실
  ablation은 존재하지 않는다**(§4.23d). 8k 프로브의 arm 순위는 60k에서 뒤집혔으므로
  (§4.23c) 8k 스크리닝 결과를 60k 인과 주장으로 승격하지 않는다.
- `v9-edge-60k`(edge 단독 60k)는 step 23100/60000에서 원인 미상으로 죽었고 재개하지 않았다
  (§4.23e). 32프레임 drift eval도 v9 계열에서는 미실행 — T0-4 수치는 `v8-teacherfree-60k`
  기준이다.
- 4090 wall-clock은 확정됐지만(§4.19f) **Jetson Orin latency·energy/frame은 미측정**이고,
  희소 경로는 compile 불가라 compiled dense와 실촬 평균에서 동률 — Triton 블록 희소 커널
  전까지 시스템 기여 주장은 저활동 구간(active < 15%)에 한정된다.
- Kendall auto-loss-weight는 §4.6 붕괴 사건으로 **사용 중단** — 트리비얼 최솟값이 있는 loss 항(temporal_loss 등)엔 부적합함이 실증됨. 재시도하려면 항별 클램프 범위를 훨씬 좁히거나(예: [-2,2]), 그런 항은 auto-weight 대상에서 제외해야 함.
