# R3 실행 계획 — 현재 제출본 기준의 리뷰 통합

2026-09-07. 사용자 요청: 우선순위대로 보강. R1/R2 자체 평가는 당시 초안의 기록이며 외부 심사 판정이 아니다.

1. R1 major 9/minor 15, R2 major 2/minor 14를 현재 제출본에 연결한다. 해결, 부분, 범위 제외,
   저자 확인을 구분하며 주장 철회를 실험적 해결로 표시하지 않는다.
2. 과거 초안의 L1024 v9/20 대 8 표본 오표기, metric scale 해석, 동등성 해석, VDA causality를 정정한다.
   원본은 `work_dirs/paper_revision_r3/before/`에 보존했다. 원시 결과와 기존 freeze는 변경하지 않는다.
3. 이미 저장한 최종 L8/L256의 모든 8개 모델 scale 통계를 전수 집계한다. 이는 결과 개봉 후 추가한
   기술적/탐색적 분석이며 사전 지정한 primary endpoint가 아니다. 모델/threshold 선택에 쓰지 않는다.
   metric 모델은 no-fit, 상대 모델은 median gauge의 depth 변환에서 계산된 통계를 사용한다.
   시퀀스 안에서 clip 평균, 이후 네 시퀀스 균형 평균; 모든 시퀀스·실패를 유지한다.
   서로 다른 L8/L256의 영상 범위 차이 때문에 평균 차이를 상태 drift의 인과 효과로 해석하지 않는다.
4. 기존 VDA dumps/실행기/로컬 upstream의 시간 문맥과 provenance를 감사한다. 현재 frozen primary에
   임의로 추가하거나, 과거 데이터 수치를 현재 final 표에 섞지 않는다. 조건 미충족 시 historical
   supplement와 명확한 미해결 항목으로 남긴다. 새 실행은 이 감사에서 하지 않는다.
5. 학습 기록·평가식·수학적 기여와 적용 조건을 본문/부록으로 보강하고 PDF·테스트·패키지를 재검증한다.

새 학습, 새로운 모델 추론, 외부 제출·업로드, 저자 대신의 권리/윤리 승인, 독립 인간 수학 검토는 하지 않는다.
